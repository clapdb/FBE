//------------------------------------------------------------------------------
// Automatically generated by the Fast Binary Encoding compiler, do not modify!
// https://github.com/chronoxor/FastBinaryEncoding
// Source: variants.fbe
// FBE version: 1.10.0.0
//------------------------------------------------------------------------------

package variants;

public class Value implements Comparable<Object>
{
    public V v = new V();

    public static final long fbeTypeConst = 2;
    public long fbeType() { return fbeTypeConst; }

    public Value() {}

    public Value(V v)
    {
        this.v = v;
    }

    public Value(Value other)
    {
        this.v = other.v;
    }

    public Value clone()
    {
        // Serialize the struct to the FBE stream
        var writer = new variants.fbe.ValueModel();
        writer.serialize(this);

        // Deserialize the struct from the FBE stream
        var reader = new variants.fbe.ValueModel();
        reader.attach(writer.getBuffer());
        return reader.deserialize();
    }

    @Override
    public int compareTo(Object other)
    {
        if (other == null)
            return -1;

        if (!Value.class.isAssignableFrom(other.getClass()))
            return -1;

        final Value obj = (Value)other;

        int result = 0;
        return result;
    }

    @Override
    public boolean equals(Object other)
    {
        if (other == null)
            return false;

        if (!Value.class.isAssignableFrom(other.getClass()))
            return false;

        final Value obj = (Value)other;

        return true;
    }

    @Override
    public int hashCode()
    {
        int hash = 17;
        return hash;
    }

    @Override
    public String toString()
    {
        var sb = new StringBuilder();
        sb.append("Value(");
        sb.append("v="); sb.append(v);
        sb.append(")");
        return sb.toString();
    }

    public String toJson() { return variants.fbe.Json.getEngine().toJson(this); }
    public static Value fromJson(String json) { return variants.fbe.Json.getEngine().fromJson(json, Value.class); }
}
