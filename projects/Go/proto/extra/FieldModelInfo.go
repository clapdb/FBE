//------------------------------------------------------------------------------
// Automatically generated by the Fast Binary Encoding compiler, do not modify!
// https://github.com/chronoxor/FastBinaryEncoding
// Source: extra.fbe
// FBE version: 1.11.0.0
//------------------------------------------------------------------------------

package extra

import "errors"
import "fbeproj/proto/fbe"

// Workaround for Go unused imports issue
var _ = errors.New
var _ = fbe.Version

// Fast Binary Encoding Info field model
type FieldModelInfo struct {
    // Field model buffer
    buffer *fbe.Buffer
    // Field model buffer offset
    offset int

    Info *fbe.FieldModelString
    Extra *FieldModelPtrExtra
    Extras *FieldModelVectorPtrExtra
    Extras1 *FieldModelVectorPtrExtra
}

// Create a new Info field model
func NewFieldModelInfo(buffer *fbe.Buffer, offset int) *FieldModelInfo {
    fbeResult := FieldModelInfo{buffer: buffer, offset: offset}
    fbeResult.Info = fbe.NewFieldModelString(buffer, 4 + 4)
    fbeResult.Extra = NewFieldModelPtrExtra(buffer, fbeResult.Info.FBEOffset() + fbeResult.Info.FBESize())
    fbeResult.Extras = NewFieldModelVectorPtrExtra(buffer, fbeResult.Extra.FBEOffset() + fbeResult.Extra.FBESize())
    fbeResult.Extras1 = NewFieldModelVectorPtrExtra(buffer, fbeResult.Extras.FBEOffset() + fbeResult.Extras.FBESize())
    return &fbeResult
}

// Get the field size
func (fm *FieldModelInfo) FBESize() int { return 4 }

// Get the field body size
func (fm *FieldModelInfo) FBEBody() int {
    fbeResult := 4 + 4 +
        fm.Info.FBESize() +
        fm.Extra.FBESize() +
        fm.Extras.FBESize() +
        fm.Extras1.FBESize() +
        0
    return fbeResult
}

// Get the field extra size
func (fm *FieldModelInfo) FBEExtra() int {
    if (fm.buffer.Offset() + fm.FBEOffset() + fm.FBESize()) > fm.buffer.Size() {
        return 0
    }

    fbeStructOffset := int(fbe.ReadUInt32(fm.buffer.Data(), fm.buffer.Offset() + fm.FBEOffset()))
    if (fbeStructOffset == 0) || ((fm.buffer.Offset() + fbeStructOffset + 4) > fm.buffer.Size()) {
        return 0
    }

    fm.buffer.Shift(fbeStructOffset)

    fbeResult := fm.FBEBody() +
        fm.Info.FBEExtra() +
        fm.Extra.FBEExtra() +
        fm.Extras.FBEExtra() +
        fm.Extras1.FBEExtra() +
        0

    fm.buffer.Unshift(fbeStructOffset)

    return fbeResult
}

// Get the field type
func (fm *FieldModelInfo) FBEType() int { return 1 }

// Get the field offset
func (fm *FieldModelInfo) FBEOffset() int { return fm.offset }
// Set the field offset
func (fm *FieldModelInfo) SetFBEOffset(value int) { fm.offset = value }

// Shift the current field offset
func (fm *FieldModelInfo) FBEShift(size int) { fm.offset += size }
// Unshift the current field offset
func (fm *FieldModelInfo) FBEUnshift(size int) { fm.offset -= size }

// Check if the struct value is valid
func (fm *FieldModelInfo) Verify() bool { return fm.VerifyType(true) }

// Check if the struct value and its type are valid
func (fm *FieldModelInfo) VerifyType(fbeVerifyType bool) bool {
    if (fm.buffer.Offset() + fm.FBEOffset() + fm.FBESize()) > fm.buffer.Size() {
        return true
    }

    fbeStructOffset := int(fbe.ReadUInt32(fm.buffer.Data(), fm.buffer.Offset() + fm.FBEOffset()))
    if (fbeStructOffset == 0) || ((fm.buffer.Offset() + fbeStructOffset + 4 + 4) > fm.buffer.Size()) {
        return false
    }

    fbeStructSize := int(fbe.ReadUInt32(fm.buffer.Data(), fm.buffer.Offset() + fbeStructOffset))
    if fbeStructSize < (4 + 4) {
        return false
    }

    fbeStructType := int(fbe.ReadUInt32(fm.buffer.Data(), fm.buffer.Offset() + fbeStructOffset + 4))
    if fbeVerifyType && (fbeStructType != fm.FBEType()) {
        return false
    }

    fm.buffer.Shift(fbeStructOffset)
    fbeResult := fm.VerifyFields(fbeStructSize)
    fm.buffer.Unshift(fbeStructOffset)
    return fbeResult
}

// // Check if the struct value fields are valid
func (fm *FieldModelInfo) VerifyFields(fbeStructSize int) bool {
    fbeCurrentSize := 4 + 4

    if (fbeCurrentSize + fm.Info.FBESize()) > fbeStructSize {
        return true
    }
    if !fm.Info.Verify() {
        return false
    }
    fbeCurrentSize += fm.Info.FBESize()

    if (fbeCurrentSize + fm.Extra.FBESize()) > fbeStructSize {
        return true
    }
    if !fm.Extra.Verify() {
        return false
    }
    fbeCurrentSize += fm.Extra.FBESize()

    if (fbeCurrentSize + fm.Extras.FBESize()) > fbeStructSize {
        return true
    }
    if !fm.Extras.Verify() {
        return false
    }
    fbeCurrentSize += fm.Extras.FBESize()

    if (fbeCurrentSize + fm.Extras1.FBESize()) > fbeStructSize {
        return true
    }
    if !fm.Extras1.Verify() {
        return false
    }
    fbeCurrentSize += fm.Extras1.FBESize()

    return true
}

// Get the struct value (begin phase)
func (fm *FieldModelInfo) GetBegin() (int, error) {
    if (fm.buffer.Offset() + fm.FBEOffset() + fm.FBESize()) > fm.buffer.Size() {
        return 0, nil
    }

    fbeStructOffset := int(fbe.ReadUInt32(fm.buffer.Data(), fm.buffer.Offset() + fm.FBEOffset()))
    if (fbeStructOffset == 0) || ((fm.buffer.Offset() + fbeStructOffset + 4 + 4) > fm.buffer.Size()) {
        return 0, errors.New("model is broken")
    }

    fbeStructSize := int(fbe.ReadUInt32(fm.buffer.Data(), fm.buffer.Offset() + fbeStructOffset))
    if fbeStructSize < (4 + 4) {
        return 0, errors.New("model is broken")
    }

    fm.buffer.Shift(fbeStructOffset)
    return fbeStructOffset, nil
}

// Get the struct value (end phase)
func (fm *FieldModelInfo) GetEnd(fbeBegin int) {
    fm.buffer.Unshift(fbeBegin)
}

// Get the struct value
func (fm *FieldModelInfo) Get() (*Info, error) {
    fbeResult := NewInfo()
    return fbeResult, fm.GetValue(fbeResult)
}

// Get the struct value by the given pointer
func (fm *FieldModelInfo) GetValue(fbeValue *Info) error {
    fbeBegin, err := fm.GetBegin()
    if fbeBegin == 0 {
        return err
    }

    fbeStructSize := int(fbe.ReadUInt32(fm.buffer.Data(), fm.buffer.Offset()))
    fm.GetFields(fbeValue, fbeStructSize)
    fm.GetEnd(fbeBegin)
    return nil
}

// Get the struct fields values
func (fm *FieldModelInfo) GetFields(fbeValue *Info, fbeStructSize int) {
    fbeCurrentSize := 4 + 4

    if (fbeCurrentSize + fm.Info.FBESize()) <= fbeStructSize {
        fbeValue.Info, _ = fm.Info.Get()
    } else {
        fbeValue.Info = ""
    }
    fbeCurrentSize += fm.Info.FBESize()

    if (fbeCurrentSize + fm.Extra.FBESize()) <= fbeStructSize {
        fbeValue.Extra, _ = fm.Extra.Get()
    } else {
        fbeValue.Extra = nil
    }
    fbeCurrentSize += fm.Extra.FBESize()

    if (fbeCurrentSize + fm.Extras.FBESize()) <= fbeStructSize {
        fbeValue.Extras, _ = fm.Extras.Get()
    } else {
        fbeValue.Extras = make([]*Extra, 0)
    }
    fbeCurrentSize += fm.Extras.FBESize()

    if (fbeCurrentSize + fm.Extras1.FBESize()) <= fbeStructSize {
        fbeValue.Extras1, _ = fm.Extras1.Get()
    } else {
        fbeValue.Extras1 = make([]*Extra, 0)
    }
    fbeCurrentSize += fm.Extras1.FBESize()
}

// Set the struct value (begin phase)
func (fm *FieldModelInfo) SetBegin() (int, error) {
    if (fm.buffer.Offset() + fm.FBEOffset() + fm.FBESize()) > fm.buffer.Size() {
        return 0, errors.New("model is broken")
    }

    fbeStructSize := fm.FBEBody()
    fbeStructOffset := fm.buffer.Allocate(fbeStructSize) - fm.buffer.Offset()
    if (fbeStructOffset <= 0) || ((fm.buffer.Offset() + fbeStructOffset + fbeStructSize) > fm.buffer.Size()) {
        return 0, errors.New("model is broken")
    }

    fbe.WriteUInt32(fm.buffer.Data(), fm.buffer.Offset() + fm.FBEOffset(), uint32(fbeStructOffset))
    fbe.WriteUInt32(fm.buffer.Data(), fm.buffer.Offset() + fbeStructOffset, uint32(fbeStructSize))
    fbe.WriteUInt32(fm.buffer.Data(), fm.buffer.Offset() + fbeStructOffset + 4, uint32(fm.FBEType()))

    fm.buffer.Shift(fbeStructOffset)
    return fbeStructOffset, nil
}

// Set the struct value (end phase)
func (fm *FieldModelInfo) SetEnd(fbeBegin int) {
    fm.buffer.Unshift(fbeBegin)
}

// Set the struct value
func (fm *FieldModelInfo) Set(fbeValue *Info) error {
    fbeBegin, err := fm.SetBegin()
    if fbeBegin == 0 {
        return err
    }

    err = fm.SetFields(fbeValue)
    fm.SetEnd(fbeBegin)
    return err
}

// Set the struct fields values
func (fm *FieldModelInfo) SetFields(fbeValue *Info) error {
    var err error = nil

    if err = fm.Info.Set(fbeValue.Info); err != nil {
        return err
    }
    if err = fm.Extra.Set(fbeValue.Extra); err != nil {
        return err
    }
    if err = fm.Extras.Set(fbeValue.Extras); err != nil {
        return err
    }
    if err = fm.Extras1.Set(fbeValue.Extras1); err != nil {
        return err
    }
    return err
}
