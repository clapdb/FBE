//------------------------------------------------------------------------------
// Automatically generated by the Fast Binary Encoding compiler, do not modify!
// https://github.com/chronoxor/FastBinaryEncoding
// Source: pkg.fbe
// FBE version: 1.11.0.0
//------------------------------------------------------------------------------

package pkg

import "errors"
import "fbeproj/proto/fbe"
import "fbeproj/proto/osa"

// Workaround for Go unused imports issue
var _ = errors.New
var _ = fbe.Version
var _ = osa.Version

// Fast Binary Encoding Detail field model
type FieldModelDetail struct {
    // Field model buffer
    buffer *fbe.Buffer
    // Field model buffer offset
    offset int

    Extrav *FieldModelVectorosaExtra
    Extram *FieldModelMapInt32osaExtra
}

// Create a new Detail field model
func NewFieldModelDetail(buffer *fbe.Buffer, offset int) *FieldModelDetail {
    fbeResult := FieldModelDetail{buffer: buffer, offset: offset}
    fbeResult.Extrav = NewFieldModelVectorosaExtra(buffer, 4 + 4)
    fbeResult.Extram = NewFieldModelMapInt32osaExtra(buffer, fbeResult.Extrav.FBEOffset() + fbeResult.Extrav.FBESize())
    return &fbeResult
}

// Get the field size
func (fm *FieldModelDetail) FBESize() int { return 4 }

// Get the field body size
func (fm *FieldModelDetail) FBEBody() int {
    fbeResult := 4 + 4 +
        fm.Extrav.FBESize() +
        fm.Extram.FBESize() +
        0
    return fbeResult
}

// Get the field extra size
func (fm *FieldModelDetail) FBEExtra() int {
    if (fm.buffer.Offset() + fm.FBEOffset() + fm.FBESize()) > fm.buffer.Size() {
        return 0
    }

    fbeStructOffset := int(fbe.ReadUInt32(fm.buffer.Data(), fm.buffer.Offset() + fm.FBEOffset()))
    if (fbeStructOffset == 0) || ((fm.buffer.Offset() + fbeStructOffset + 4) > fm.buffer.Size()) {
        return 0
    }

    fm.buffer.Shift(fbeStructOffset)

    fbeResult := fm.FBEBody() +
        fm.Extrav.FBEExtra() +
        fm.Extram.FBEExtra() +
        0

    fm.buffer.Unshift(fbeStructOffset)

    return fbeResult
}

// Get the field type
func (fm *FieldModelDetail) FBEType() int { return 2 }

// Get the field offset
func (fm *FieldModelDetail) FBEOffset() int { return fm.offset }
// Set the field offset
func (fm *FieldModelDetail) SetFBEOffset(value int) { fm.offset = value }

// Shift the current field offset
func (fm *FieldModelDetail) FBEShift(size int) { fm.offset += size }
// Unshift the current field offset
func (fm *FieldModelDetail) FBEUnshift(size int) { fm.offset -= size }

// Check if the struct value is valid
func (fm *FieldModelDetail) Verify() bool { return fm.VerifyType(true) }

// Check if the struct value and its type are valid
func (fm *FieldModelDetail) VerifyType(fbeVerifyType bool) bool {
    if (fm.buffer.Offset() + fm.FBEOffset() + fm.FBESize()) > fm.buffer.Size() {
        return true
    }

    fbeStructOffset := int(fbe.ReadUInt32(fm.buffer.Data(), fm.buffer.Offset() + fm.FBEOffset()))
    if (fbeStructOffset == 0) || ((fm.buffer.Offset() + fbeStructOffset + 4 + 4) > fm.buffer.Size()) {
        return false
    }

    fbeStructSize := int(fbe.ReadUInt32(fm.buffer.Data(), fm.buffer.Offset() + fbeStructOffset))
    if fbeStructSize < (4 + 4) {
        return false
    }

    fbeStructType := int(fbe.ReadUInt32(fm.buffer.Data(), fm.buffer.Offset() + fbeStructOffset + 4))
    if fbeVerifyType && (fbeStructType != fm.FBEType()) {
        return false
    }

    fm.buffer.Shift(fbeStructOffset)
    fbeResult := fm.VerifyFields(fbeStructSize)
    fm.buffer.Unshift(fbeStructOffset)
    return fbeResult
}

// // Check if the struct value fields are valid
func (fm *FieldModelDetail) VerifyFields(fbeStructSize int) bool {
    fbeCurrentSize := 4 + 4

    if (fbeCurrentSize + fm.Extrav.FBESize()) > fbeStructSize {
        return true
    }
    if !fm.Extrav.Verify() {
        return false
    }
    fbeCurrentSize += fm.Extrav.FBESize()

    if (fbeCurrentSize + fm.Extram.FBESize()) > fbeStructSize {
        return true
    }
    if !fm.Extram.Verify() {
        return false
    }
    fbeCurrentSize += fm.Extram.FBESize()

    return true
}

// Get the struct value (begin phase)
func (fm *FieldModelDetail) GetBegin() (int, error) {
    if (fm.buffer.Offset() + fm.FBEOffset() + fm.FBESize()) > fm.buffer.Size() {
        return 0, nil
    }

    fbeStructOffset := int(fbe.ReadUInt32(fm.buffer.Data(), fm.buffer.Offset() + fm.FBEOffset()))
    if (fbeStructOffset == 0) || ((fm.buffer.Offset() + fbeStructOffset + 4 + 4) > fm.buffer.Size()) {
        return 0, errors.New("model is broken")
    }

    fbeStructSize := int(fbe.ReadUInt32(fm.buffer.Data(), fm.buffer.Offset() + fbeStructOffset))
    if fbeStructSize < (4 + 4) {
        return 0, errors.New("model is broken")
    }

    fm.buffer.Shift(fbeStructOffset)
    return fbeStructOffset, nil
}

// Get the struct value (end phase)
func (fm *FieldModelDetail) GetEnd(fbeBegin int) {
    fm.buffer.Unshift(fbeBegin)
}

// Get the struct value
func (fm *FieldModelDetail) Get() (*Detail, error) {
    fbeResult := NewDetail()
    return fbeResult, fm.GetValue(fbeResult)
}

// Get the struct value by the given pointer
func (fm *FieldModelDetail) GetValue(fbeValue *Detail) error {
    fbeBegin, err := fm.GetBegin()
    if fbeBegin == 0 {
        return err
    }

    fbeStructSize := int(fbe.ReadUInt32(fm.buffer.Data(), fm.buffer.Offset()))
    fm.GetFields(fbeValue, fbeStructSize)
    fm.GetEnd(fbeBegin)
    return nil
}

// Get the struct fields values
func (fm *FieldModelDetail) GetFields(fbeValue *Detail, fbeStructSize int) {
    fbeCurrentSize := 4 + 4

    if (fbeCurrentSize + fm.Extrav.FBESize()) <= fbeStructSize {
        fbeValue.Extrav, _ = fm.Extrav.Get()
    } else {
        fbeValue.Extrav = make([]osa.Extra, 0)
    }
    fbeCurrentSize += fm.Extrav.FBESize()

    if (fbeCurrentSize + fm.Extram.FBESize()) <= fbeStructSize {
        fbeValue.Extram, _ = fm.Extram.Get()
    } else {
        fbeValue.Extram = make(map[int32]osa.Extra)
    }
    fbeCurrentSize += fm.Extram.FBESize()
}

// Set the struct value (begin phase)
func (fm *FieldModelDetail) SetBegin() (int, error) {
    if (fm.buffer.Offset() + fm.FBEOffset() + fm.FBESize()) > fm.buffer.Size() {
        return 0, errors.New("model is broken")
    }

    fbeStructSize := fm.FBEBody()
    fbeStructOffset := fm.buffer.Allocate(fbeStructSize) - fm.buffer.Offset()
    if (fbeStructOffset <= 0) || ((fm.buffer.Offset() + fbeStructOffset + fbeStructSize) > fm.buffer.Size()) {
        return 0, errors.New("model is broken")
    }

    fbe.WriteUInt32(fm.buffer.Data(), fm.buffer.Offset() + fm.FBEOffset(), uint32(fbeStructOffset))
    fbe.WriteUInt32(fm.buffer.Data(), fm.buffer.Offset() + fbeStructOffset, uint32(fbeStructSize))
    fbe.WriteUInt32(fm.buffer.Data(), fm.buffer.Offset() + fbeStructOffset + 4, uint32(fm.FBEType()))

    fm.buffer.Shift(fbeStructOffset)
    return fbeStructOffset, nil
}

// Set the struct value (end phase)
func (fm *FieldModelDetail) SetEnd(fbeBegin int) {
    fm.buffer.Unshift(fbeBegin)
}

// Set the struct value
func (fm *FieldModelDetail) Set(fbeValue *Detail) error {
    fbeBegin, err := fm.SetBegin()
    if fbeBegin == 0 {
        return err
    }

    err = fm.SetFields(fbeValue)
    fm.SetEnd(fbeBegin)
    return err
}

// Set the struct fields values
func (fm *FieldModelDetail) SetFields(fbeValue *Detail) error {
    var err error = nil

    if err = fm.Extrav.Set(fbeValue.Extrav); err != nil {
        return err
    }
    if err = fm.Extram.Set(fbeValue.Extram); err != nil {
        return err
    }
    return err
}
