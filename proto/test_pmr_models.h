//------------------------------------------------------------------------------
// Automatically generated by the Fast Binary Encoding compiler, do not modify!
// https://github.com/chronoxor/FastBinaryEncoding
// Source: test.fbe
// FBE version: 1.11.0.0
//------------------------------------------------------------------------------

#pragma once

#ifdef isset
#undef isset
#endif

#if defined(__clang__)
#pragma clang system_header
#elif defined(__GNUC__)
#pragma GCC system_header
#elif defined(_MSC_VER)
#pragma system_header
#endif

#include "fbe_models.h"

#include "test_pmr.h"

#include "proto_pmr_models.h"

namespace FBE {

// Fast Binary Encoding ::test_pmr::EnumSimple field model
template <>
class FieldModel<::test_pmr::EnumSimple> : public FieldModelBase<::test_pmr::EnumSimple, int32_t>
{
public:
    using FieldModelBase<::test_pmr::EnumSimple, int32_t>::FieldModelBase;
};

// Fast Binary Encoding ::test_pmr::EnumTyped field model
template <>
class FieldModel<::test_pmr::EnumTyped> : public FieldModelBase<::test_pmr::EnumTyped, uint8_t>
{
public:
    using FieldModelBase<::test_pmr::EnumTyped, uint8_t>::FieldModelBase;
};

// Fast Binary Encoding ::test_pmr::EnumEmpty field model
template <>
class FieldModel<::test_pmr::EnumEmpty> : public FieldModelBase<::test_pmr::EnumEmpty, int32_t>
{
public:
    using FieldModelBase<::test_pmr::EnumEmpty, int32_t>::FieldModelBase;
};

// Fast Binary Encoding ::test_pmr::FlagsSimple field model
template <>
class FieldModel<::test_pmr::FlagsSimple> : public FieldModelBase<::test_pmr::FlagsSimple, int32_t>
{
public:
    using FieldModelBase<::test_pmr::FlagsSimple, int32_t>::FieldModelBase;
};

// Fast Binary Encoding ::test_pmr::FlagsTyped field model
template <>
class FieldModel<::test_pmr::FlagsTyped> : public FieldModelBase<::test_pmr::FlagsTyped, uint64_t>
{
public:
    using FieldModelBase<::test_pmr::FlagsTyped, uint64_t>::FieldModelBase;
};

// Fast Binary Encoding ::test_pmr::FlagsEmpty field model
template <>
class FieldModel<::test_pmr::FlagsEmpty> : public FieldModelBase<::test_pmr::FlagsEmpty, int32_t>
{
public:
    using FieldModelBase<::test_pmr::FlagsEmpty, int32_t>::FieldModelBase;
};

// Fast Binary Encoding ::test_pmr::StructSimple field model
template <>
class FieldModel<::test_pmr::StructSimple>
{
public:
    FieldModel(FBEBuffer& buffer, size_t offset) noexcept;

    // Get the field offset
    size_t fbe_offset() const noexcept { return _offset; }
    // Get the field size
    size_t fbe_size() const noexcept { return 4; }
    // Get the field body size
    size_t fbe_body() const noexcept;
    // Get the field extra size
    size_t fbe_extra() const noexcept;
    // Get the field type
    static constexpr size_t fbe_type() noexcept { return 110; }

    // Shift the current field offset
    void fbe_shift(size_t size) noexcept { _offset += size; }
    // Unshift the current field offset
    void fbe_unshift(size_t size) noexcept { _offset -= size; }

    // Check if the struct value is valid
    bool verify(bool fbe_verify_type = true) const noexcept;
    // Check if the struct fields are valid
    bool verify_fields(size_t fbe_struct_size) const noexcept;

    // Get the struct value (begin phase)
    size_t get_begin() const noexcept;
    // Get the struct value (end phase)
    void get_end(size_t fbe_begin) const noexcept;

    // Get the struct value
    void get(::test_pmr::StructSimple& fbe_value, pmr::memory_resource* resource) const noexcept;
    // Get the struct fields values
    void get_fields(::test_pmr::StructSimple& fbe_value, size_t fbe_struct_size, pmr::memory_resource* resource) const noexcept;

    // Set the struct value (begin phase)
    size_t set_begin();
    // Set the struct value (end phase)
    void set_end(size_t fbe_begin);

    // Set the struct value
    void set(const ::test_pmr::StructSimple& fbe_value, pmr::memory_resource* resource) noexcept;
    // Set the struct fields values
    void set_fields(const ::test_pmr::StructSimple& fbe_value, pmr::memory_resource* resource) noexcept;

private:
    FBEBuffer& _buffer;
    size_t _offset;

public:
    FieldModel<int32_t> id;
    FieldModel<bool> f1;
    FieldModel<bool> f2;
    FieldModel<uint8_t> f3;
    FieldModel<uint8_t> f4;
    FieldModel<char> f5;
    FieldModel<char> f6;
    FieldModel<wchar_t> f7;
    FieldModel<wchar_t> f8;
    FieldModel<int8_t> f9;
    FieldModel<int8_t> f10;
    FieldModel<uint8_t> f11;
    FieldModel<uint8_t> f12;
    FieldModel<int16_t> f13;
    FieldModel<int16_t> f14;
    FieldModel<uint16_t> f15;
    FieldModel<uint16_t> f16;
    FieldModel<int32_t> f17;
    FieldModel<int32_t> f18;
    FieldModel<uint32_t> f19;
    FieldModel<uint32_t> f20;
    FieldModel<int64_t> f21;
    FieldModel<int64_t> f22;
    FieldModel<uint64_t> f23;
    FieldModel<uint64_t> f24;
    FieldModel<float> f25;
    FieldModel<float> f26;
    FieldModel<double> f27;
    FieldModel<double> f28;
    FieldModel<FBE::decimal_t> f29;
    FieldModel<FBE::decimal_t> f30;
    FieldModel<ArenaString> f31;
    FieldModel<ArenaString> f32;
    FieldModel<uint64_t> f33;
    FieldModel<uint64_t> f34;
    FieldModel<uint64_t> f35;
    FieldModel<FBE::uuid_t> f36;
    FieldModel<FBE::uuid_t> f37;
    FieldModel<FBE::uuid_t> f38;
    FieldModel<::proto_pmr::OrderSide> f39;
    FieldModel<::proto_pmr::OrderType> f40;
    FieldModel<::proto_pmr::Order> f41;
    FieldModel<::proto_pmr::Balance> f42;
    FieldModel<::proto_pmr::State> f43;
    FieldModel<::proto_pmr::Account> f44;
};

namespace test_pmr {

// Fast Binary Encoding StructSimple model
class StructSimpleModel : public FBE::Model
{
public:
    StructSimpleModel() : model(this->buffer(), 4) {}
    StructSimpleModel(const std::shared_ptr<FBEBuffer>& buffer) : FBE::Model(buffer), model(this->buffer(), 4) {}

    // Get the model size
    size_t fbe_size() const noexcept { return model.fbe_size() + model.fbe_extra(); }
    // Get the model type
    static constexpr size_t fbe_type() noexcept { return FieldModel<::test_pmr::StructSimple>::fbe_type(); }

    // Check if the struct value is valid
    bool verify();

    // Create a new model (begin phase)
    size_t create_begin();
    // Create a new model (end phase)
    size_t create_end(size_t fbe_begin);

    // Serialize the struct value
    size_t serialize(const ::test_pmr::StructSimple& value, pmr::memory_resource* resource);
    // Deserialize the struct value
    size_t deserialize(::test_pmr::StructSimple& value, pmr::memory_resource* resource) const noexcept;

    // Move to the next struct value
    void next(size_t prev) noexcept { model.fbe_shift(prev); }

public:
    FieldModel<::test_pmr::StructSimple> model;
};

} // namespace test_pmr

template<>
struct model_of<::test_pmr::StructSimple> {
using type = test_pmr::StructSimpleModel;
};


// Fast Binary Encoding ::test_pmr::StructOptional field model
template <>
class FieldModel<::test_pmr::StructOptional>
{
public:
    FieldModel(FBEBuffer& buffer, size_t offset) noexcept;

    // Get the field offset
    size_t fbe_offset() const noexcept { return _offset; }
    // Get the field size
    size_t fbe_size() const noexcept { return 4; }
    // Get the field body size
    size_t fbe_body() const noexcept;
    // Get the field extra size
    size_t fbe_extra() const noexcept;
    // Get the field type
    static constexpr size_t fbe_type() noexcept { return 111; }

    // Shift the current field offset
    void fbe_shift(size_t size) noexcept { _offset += size; }
    // Unshift the current field offset
    void fbe_unshift(size_t size) noexcept { _offset -= size; }

    // Check if the struct value is valid
    bool verify(bool fbe_verify_type = true) const noexcept;
    // Check if the struct fields are valid
    bool verify_fields(size_t fbe_struct_size) const noexcept;

    // Get the struct value (begin phase)
    size_t get_begin() const noexcept;
    // Get the struct value (end phase)
    void get_end(size_t fbe_begin) const noexcept;

    // Get the struct value
    void get(::test_pmr::StructOptional& fbe_value, pmr::memory_resource* resource) const noexcept;
    // Get the struct fields values
    void get_fields(::test_pmr::StructOptional& fbe_value, size_t fbe_struct_size, pmr::memory_resource* resource) const noexcept;

    // Set the struct value (begin phase)
    size_t set_begin();
    // Set the struct value (end phase)
    void set_end(size_t fbe_begin);

    // Set the struct value
    void set(const ::test_pmr::StructOptional& fbe_value, pmr::memory_resource* resource) noexcept;
    // Set the struct fields values
    void set_fields(const ::test_pmr::StructOptional& fbe_value, pmr::memory_resource* resource) noexcept;

private:
    FBEBuffer& _buffer;
    size_t _offset;

public:
    FieldModel<::test_pmr::StructSimple> parent;
    FieldModel<std::optional<bool>> f100;
    FieldModel<std::optional<bool>> f101;
    FieldModel<std::optional<bool>> f102;
    FieldModel<std::optional<uint8_t>> f103;
    FieldModel<std::optional<uint8_t>> f104;
    FieldModel<std::optional<uint8_t>> f105;
    FieldModel<std::optional<char>> f106;
    FieldModel<std::optional<char>> f107;
    FieldModel<std::optional<char>> f108;
    FieldModel<std::optional<wchar_t>> f109;
    FieldModel<std::optional<wchar_t>> f110;
    FieldModel<std::optional<wchar_t>> f111;
    FieldModel<std::optional<int8_t>> f112;
    FieldModel<std::optional<int8_t>> f113;
    FieldModel<std::optional<int8_t>> f114;
    FieldModel<std::optional<uint8_t>> f115;
    FieldModel<std::optional<uint8_t>> f116;
    FieldModel<std::optional<uint8_t>> f117;
    FieldModel<std::optional<int16_t>> f118;
    FieldModel<std::optional<int16_t>> f119;
    FieldModel<std::optional<int16_t>> f120;
    FieldModel<std::optional<uint16_t>> f121;
    FieldModel<std::optional<uint16_t>> f122;
    FieldModel<std::optional<uint16_t>> f123;
    FieldModel<std::optional<int32_t>> f124;
    FieldModel<std::optional<int32_t>> f125;
    FieldModel<std::optional<int32_t>> f126;
    FieldModel<std::optional<uint32_t>> f127;
    FieldModel<std::optional<uint32_t>> f128;
    FieldModel<std::optional<uint32_t>> f129;
    FieldModel<std::optional<int64_t>> f130;
    FieldModel<std::optional<int64_t>> f131;
    FieldModel<std::optional<int64_t>> f132;
    FieldModel<std::optional<uint64_t>> f133;
    FieldModel<std::optional<uint64_t>> f134;
    FieldModel<std::optional<uint64_t>> f135;
    FieldModel<std::optional<float>> f136;
    FieldModel<std::optional<float>> f137;
    FieldModel<std::optional<float>> f138;
    FieldModel<std::optional<double>> f139;
    FieldModel<std::optional<double>> f140;
    FieldModel<std::optional<double>> f141;
    FieldModel<std::optional<FBE::decimal_t>> f142;
    FieldModel<std::optional<FBE::decimal_t>> f143;
    FieldModel<std::optional<FBE::decimal_t>> f144;
    FieldModel<std::optional<ArenaString>> f145;
    FieldModel<std::optional<ArenaString>> f146;
    FieldModel<std::optional<ArenaString>> f147;
    FieldModel<std::optional<uint64_t>> f148;
    FieldModel<std::optional<uint64_t>> f149;
    FieldModel<std::optional<uint64_t>> f150;
    FieldModel<std::optional<FBE::uuid_t>> f151;
    FieldModel<std::optional<FBE::uuid_t>> f152;
    FieldModel<std::optional<FBE::uuid_t>> f153;
    FieldModel<std::optional<::proto_pmr::OrderSide>> f154;
    FieldModel<std::optional<::proto_pmr::OrderSide>> f155;
    FieldModel<std::optional<::proto_pmr::OrderType>> f156;
    FieldModel<std::optional<::proto_pmr::OrderType>> f157;
    FieldModel<std::optional<::proto_pmr::Order>> f158;
    FieldModel<std::optional<::proto_pmr::Order>> f159;
    FieldModel<std::optional<::proto_pmr::Balance>> f160;
    FieldModel<std::optional<::proto_pmr::Balance>> f161;
    FieldModel<std::optional<::proto_pmr::State>> f162;
    FieldModel<std::optional<::proto_pmr::State>> f163;
    FieldModel<std::optional<::proto_pmr::Account>> f164;
    FieldModel<std::optional<::proto_pmr::Account>> f165;
};

namespace test_pmr {

// Fast Binary Encoding StructOptional model
class StructOptionalModel : public FBE::Model
{
public:
    StructOptionalModel() : model(this->buffer(), 4) {}
    StructOptionalModel(const std::shared_ptr<FBEBuffer>& buffer) : FBE::Model(buffer), model(this->buffer(), 4) {}

    // Get the model size
    size_t fbe_size() const noexcept { return model.fbe_size() + model.fbe_extra(); }
    // Get the model type
    static constexpr size_t fbe_type() noexcept { return FieldModel<::test_pmr::StructOptional>::fbe_type(); }

    // Check if the struct value is valid
    bool verify();

    // Create a new model (begin phase)
    size_t create_begin();
    // Create a new model (end phase)
    size_t create_end(size_t fbe_begin);

    // Serialize the struct value
    size_t serialize(const ::test_pmr::StructOptional& value, pmr::memory_resource* resource);
    // Deserialize the struct value
    size_t deserialize(::test_pmr::StructOptional& value, pmr::memory_resource* resource) const noexcept;

    // Move to the next struct value
    void next(size_t prev) noexcept { model.fbe_shift(prev); }

public:
    FieldModel<::test_pmr::StructOptional> model;
};

} // namespace test_pmr

template<>
struct model_of<::test_pmr::StructOptional> {
using type = test_pmr::StructOptionalModel;
};


// Fast Binary Encoding ::test_pmr::StructNested field model
template <>
class FieldModel<::test_pmr::StructNested>
{
public:
    FieldModel(FBEBuffer& buffer, size_t offset) noexcept;

    // Get the field offset
    size_t fbe_offset() const noexcept { return _offset; }
    // Get the field size
    size_t fbe_size() const noexcept { return 4; }
    // Get the field body size
    size_t fbe_body() const noexcept;
    // Get the field extra size
    size_t fbe_extra() const noexcept;
    // Get the field type
    static constexpr size_t fbe_type() noexcept { return 112; }

    // Shift the current field offset
    void fbe_shift(size_t size) noexcept { _offset += size; }
    // Unshift the current field offset
    void fbe_unshift(size_t size) noexcept { _offset -= size; }

    // Check if the struct value is valid
    bool verify(bool fbe_verify_type = true) const noexcept;
    // Check if the struct fields are valid
    bool verify_fields(size_t fbe_struct_size) const noexcept;

    // Get the struct value (begin phase)
    size_t get_begin() const noexcept;
    // Get the struct value (end phase)
    void get_end(size_t fbe_begin) const noexcept;

    // Get the struct value
    void get(::test_pmr::StructNested& fbe_value, pmr::memory_resource* resource) const noexcept;
    // Get the struct fields values
    void get_fields(::test_pmr::StructNested& fbe_value, size_t fbe_struct_size, pmr::memory_resource* resource) const noexcept;

    // Set the struct value (begin phase)
    size_t set_begin();
    // Set the struct value (end phase)
    void set_end(size_t fbe_begin);

    // Set the struct value
    void set(const ::test_pmr::StructNested& fbe_value, pmr::memory_resource* resource) noexcept;
    // Set the struct fields values
    void set_fields(const ::test_pmr::StructNested& fbe_value, pmr::memory_resource* resource) noexcept;

private:
    FBEBuffer& _buffer;
    size_t _offset;

public:
    FieldModel<::test_pmr::StructOptional> parent;
    FieldModel<::test_pmr::EnumSimple> f1000;
    FieldModel<std::optional<::test_pmr::EnumSimple>> f1001;
    FieldModel<::test_pmr::EnumTyped> f1002;
    FieldModel<std::optional<::test_pmr::EnumTyped>> f1003;
    FieldModel<::test_pmr::FlagsSimple> f1004;
    FieldModel<std::optional<::test_pmr::FlagsSimple>> f1005;
    FieldModel<::test_pmr::FlagsTyped> f1006;
    FieldModel<std::optional<::test_pmr::FlagsTyped>> f1007;
    FieldModel<::test_pmr::StructSimple> f1008;
    FieldModel<std::optional<::test_pmr::StructSimple>> f1009;
    FieldModel<::test_pmr::StructOptional> f1010;
    FieldModel<std::optional<::test_pmr::StructOptional>> f1011;
};

namespace test_pmr {

// Fast Binary Encoding StructNested model
class StructNestedModel : public FBE::Model
{
public:
    StructNestedModel() : model(this->buffer(), 4) {}
    StructNestedModel(const std::shared_ptr<FBEBuffer>& buffer) : FBE::Model(buffer), model(this->buffer(), 4) {}

    // Get the model size
    size_t fbe_size() const noexcept { return model.fbe_size() + model.fbe_extra(); }
    // Get the model type
    static constexpr size_t fbe_type() noexcept { return FieldModel<::test_pmr::StructNested>::fbe_type(); }

    // Check if the struct value is valid
    bool verify();

    // Create a new model (begin phase)
    size_t create_begin();
    // Create a new model (end phase)
    size_t create_end(size_t fbe_begin);

    // Serialize the struct value
    size_t serialize(const ::test_pmr::StructNested& value, pmr::memory_resource* resource);
    // Deserialize the struct value
    size_t deserialize(::test_pmr::StructNested& value, pmr::memory_resource* resource) const noexcept;

    // Move to the next struct value
    void next(size_t prev) noexcept { model.fbe_shift(prev); }

public:
    FieldModel<::test_pmr::StructNested> model;
};

} // namespace test_pmr

template<>
struct model_of<::test_pmr::StructNested> {
using type = test_pmr::StructNestedModel;
};


// Fast Binary Encoding ::test_pmr::StructBytes field model
template <>
class FieldModel<::test_pmr::StructBytes>
{
public:
    FieldModel(FBEBuffer& buffer, size_t offset) noexcept;

    // Get the field offset
    size_t fbe_offset() const noexcept { return _offset; }
    // Get the field size
    size_t fbe_size() const noexcept { return 4; }
    // Get the field body size
    size_t fbe_body() const noexcept;
    // Get the field extra size
    size_t fbe_extra() const noexcept;
    // Get the field type
    static constexpr size_t fbe_type() noexcept { return 120; }

    // Shift the current field offset
    void fbe_shift(size_t size) noexcept { _offset += size; }
    // Unshift the current field offset
    void fbe_unshift(size_t size) noexcept { _offset -= size; }

    // Check if the struct value is valid
    bool verify(bool fbe_verify_type = true) const noexcept;
    // Check if the struct fields are valid
    bool verify_fields(size_t fbe_struct_size) const noexcept;

    // Get the struct value (begin phase)
    size_t get_begin() const noexcept;
    // Get the struct value (end phase)
    void get_end(size_t fbe_begin) const noexcept;

    // Get the struct value
    void get(::test_pmr::StructBytes& fbe_value, pmr::memory_resource* resource) const noexcept;
    // Get the struct fields values
    void get_fields(::test_pmr::StructBytes& fbe_value, size_t fbe_struct_size, pmr::memory_resource* resource) const noexcept;

    // Set the struct value (begin phase)
    size_t set_begin();
    // Set the struct value (end phase)
    void set_end(size_t fbe_begin);

    // Set the struct value
    void set(const ::test_pmr::StructBytes& fbe_value, pmr::memory_resource* resource) noexcept;
    // Set the struct fields values
    void set_fields(const ::test_pmr::StructBytes& fbe_value, pmr::memory_resource* resource) noexcept;

private:
    FBEBuffer& _buffer;
    size_t _offset;

public:
    FieldModel<FBE::pmr_buffer_t> f1;
    FieldModel<std::optional<FBE::pmr_buffer_t>> f2;
    FieldModel<std::optional<FBE::pmr_buffer_t>> f3;
};

namespace test_pmr {

// Fast Binary Encoding StructBytes model
class StructBytesModel : public FBE::Model
{
public:
    StructBytesModel() : model(this->buffer(), 4) {}
    StructBytesModel(const std::shared_ptr<FBEBuffer>& buffer) : FBE::Model(buffer), model(this->buffer(), 4) {}

    // Get the model size
    size_t fbe_size() const noexcept { return model.fbe_size() + model.fbe_extra(); }
    // Get the model type
    static constexpr size_t fbe_type() noexcept { return FieldModel<::test_pmr::StructBytes>::fbe_type(); }

    // Check if the struct value is valid
    bool verify();

    // Create a new model (begin phase)
    size_t create_begin();
    // Create a new model (end phase)
    size_t create_end(size_t fbe_begin);

    // Serialize the struct value
    size_t serialize(const ::test_pmr::StructBytes& value, pmr::memory_resource* resource);
    // Deserialize the struct value
    size_t deserialize(::test_pmr::StructBytes& value, pmr::memory_resource* resource) const noexcept;

    // Move to the next struct value
    void next(size_t prev) noexcept { model.fbe_shift(prev); }

public:
    FieldModel<::test_pmr::StructBytes> model;
};

} // namespace test_pmr

template<>
struct model_of<::test_pmr::StructBytes> {
using type = test_pmr::StructBytesModel;
};


// Fast Binary Encoding ::test_pmr::StructArray field model
template <>
class FieldModel<::test_pmr::StructArray>
{
public:
    FieldModel(FBEBuffer& buffer, size_t offset) noexcept;

    // Get the field offset
    size_t fbe_offset() const noexcept { return _offset; }
    // Get the field size
    size_t fbe_size() const noexcept { return 4; }
    // Get the field body size
    size_t fbe_body() const noexcept;
    // Get the field extra size
    size_t fbe_extra() const noexcept;
    // Get the field type
    static constexpr size_t fbe_type() noexcept { return 125; }

    // Shift the current field offset
    void fbe_shift(size_t size) noexcept { _offset += size; }
    // Unshift the current field offset
    void fbe_unshift(size_t size) noexcept { _offset -= size; }

    // Check if the struct value is valid
    bool verify(bool fbe_verify_type = true) const noexcept;
    // Check if the struct fields are valid
    bool verify_fields(size_t fbe_struct_size) const noexcept;

    // Get the struct value (begin phase)
    size_t get_begin() const noexcept;
    // Get the struct value (end phase)
    void get_end(size_t fbe_begin) const noexcept;

    // Get the struct value
    void get(::test_pmr::StructArray& fbe_value, pmr::memory_resource* resource) const noexcept;
    // Get the struct fields values
    void get_fields(::test_pmr::StructArray& fbe_value, size_t fbe_struct_size, pmr::memory_resource* resource) const noexcept;

    // Set the struct value (begin phase)
    size_t set_begin();
    // Set the struct value (end phase)
    void set_end(size_t fbe_begin);

    // Set the struct value
    void set(const ::test_pmr::StructArray& fbe_value, pmr::memory_resource* resource) noexcept;
    // Set the struct fields values
    void set_fields(const ::test_pmr::StructArray& fbe_value, pmr::memory_resource* resource) noexcept;

private:
    FBEBuffer& _buffer;
    size_t _offset;

public:
    FieldModelArray<uint8_t, 2> f1;
    FieldModelArray<std::optional<uint8_t>, 2> f2;
    FieldModelArray<FBE::pmr_buffer_t, 2> f3;
    FieldModelArray<std::optional<FBE::pmr_buffer_t>, 2> f4;
    FieldModelArray<::test_pmr::EnumSimple, 2> f5;
    FieldModelArray<std::optional<::test_pmr::EnumSimple>, 2> f6;
    FieldModelArray<::test_pmr::FlagsSimple, 2> f7;
    FieldModelArray<std::optional<::test_pmr::FlagsSimple>, 2> f8;
    FieldModelArray<::test_pmr::StructSimple, 2> f9;
    FieldModelArray<std::optional<::test_pmr::StructSimple>, 2> f10;
};

namespace test_pmr {

// Fast Binary Encoding StructArray model
class StructArrayModel : public FBE::Model
{
public:
    StructArrayModel() : model(this->buffer(), 4) {}
    StructArrayModel(const std::shared_ptr<FBEBuffer>& buffer) : FBE::Model(buffer), model(this->buffer(), 4) {}

    // Get the model size
    size_t fbe_size() const noexcept { return model.fbe_size() + model.fbe_extra(); }
    // Get the model type
    static constexpr size_t fbe_type() noexcept { return FieldModel<::test_pmr::StructArray>::fbe_type(); }

    // Check if the struct value is valid
    bool verify();

    // Create a new model (begin phase)
    size_t create_begin();
    // Create a new model (end phase)
    size_t create_end(size_t fbe_begin);

    // Serialize the struct value
    size_t serialize(const ::test_pmr::StructArray& value, pmr::memory_resource* resource);
    // Deserialize the struct value
    size_t deserialize(::test_pmr::StructArray& value, pmr::memory_resource* resource) const noexcept;

    // Move to the next struct value
    void next(size_t prev) noexcept { model.fbe_shift(prev); }

public:
    FieldModel<::test_pmr::StructArray> model;
};

} // namespace test_pmr

template<>
struct model_of<::test_pmr::StructArray> {
using type = test_pmr::StructArrayModel;
};


// Fast Binary Encoding ::test_pmr::StructVector field model
template <>
class FieldModel<::test_pmr::StructVector>
{
public:
    FieldModel(FBEBuffer& buffer, size_t offset) noexcept;

    // Get the field offset
    size_t fbe_offset() const noexcept { return _offset; }
    // Get the field size
    size_t fbe_size() const noexcept { return 4; }
    // Get the field body size
    size_t fbe_body() const noexcept;
    // Get the field extra size
    size_t fbe_extra() const noexcept;
    // Get the field type
    static constexpr size_t fbe_type() noexcept { return 130; }

    // Shift the current field offset
    void fbe_shift(size_t size) noexcept { _offset += size; }
    // Unshift the current field offset
    void fbe_unshift(size_t size) noexcept { _offset -= size; }

    // Check if the struct value is valid
    bool verify(bool fbe_verify_type = true) const noexcept;
    // Check if the struct fields are valid
    bool verify_fields(size_t fbe_struct_size) const noexcept;

    // Get the struct value (begin phase)
    size_t get_begin() const noexcept;
    // Get the struct value (end phase)
    void get_end(size_t fbe_begin) const noexcept;

    // Get the struct value
    void get(::test_pmr::StructVector& fbe_value, pmr::memory_resource* resource) const noexcept;
    // Get the struct fields values
    void get_fields(::test_pmr::StructVector& fbe_value, size_t fbe_struct_size, pmr::memory_resource* resource) const noexcept;

    // Set the struct value (begin phase)
    size_t set_begin();
    // Set the struct value (end phase)
    void set_end(size_t fbe_begin);

    // Set the struct value
    void set(const ::test_pmr::StructVector& fbe_value, pmr::memory_resource* resource) noexcept;
    // Set the struct fields values
    void set_fields(const ::test_pmr::StructVector& fbe_value, pmr::memory_resource* resource) noexcept;

private:
    FBEBuffer& _buffer;
    size_t _offset;

public:
    FieldModelVector<uint8_t> f1;
    FieldModelVector<std::optional<uint8_t>> f2;
    FieldModelVector<FBE::pmr_buffer_t> f3;
    FieldModelVector<std::optional<FBE::pmr_buffer_t>> f4;
    FieldModelVector<::test_pmr::EnumSimple> f5;
    FieldModelVector<std::optional<::test_pmr::EnumSimple>> f6;
    FieldModelVector<::test_pmr::FlagsSimple> f7;
    FieldModelVector<std::optional<::test_pmr::FlagsSimple>> f8;
    FieldModelVector<::test_pmr::StructSimple> f9;
    FieldModelVector<std::optional<::test_pmr::StructSimple>> f10;
};

namespace test_pmr {

// Fast Binary Encoding StructVector model
class StructVectorModel : public FBE::Model
{
public:
    StructVectorModel() : model(this->buffer(), 4) {}
    StructVectorModel(const std::shared_ptr<FBEBuffer>& buffer) : FBE::Model(buffer), model(this->buffer(), 4) {}

    // Get the model size
    size_t fbe_size() const noexcept { return model.fbe_size() + model.fbe_extra(); }
    // Get the model type
    static constexpr size_t fbe_type() noexcept { return FieldModel<::test_pmr::StructVector>::fbe_type(); }

    // Check if the struct value is valid
    bool verify();

    // Create a new model (begin phase)
    size_t create_begin();
    // Create a new model (end phase)
    size_t create_end(size_t fbe_begin);

    // Serialize the struct value
    size_t serialize(const ::test_pmr::StructVector& value, pmr::memory_resource* resource);
    // Deserialize the struct value
    size_t deserialize(::test_pmr::StructVector& value, pmr::memory_resource* resource) const noexcept;

    // Move to the next struct value
    void next(size_t prev) noexcept { model.fbe_shift(prev); }

public:
    FieldModel<::test_pmr::StructVector> model;
};

} // namespace test_pmr

template<>
struct model_of<::test_pmr::StructVector> {
using type = test_pmr::StructVectorModel;
};


// Fast Binary Encoding ::test_pmr::StructList field model
template <>
class FieldModel<::test_pmr::StructList>
{
public:
    FieldModel(FBEBuffer& buffer, size_t offset) noexcept;

    // Get the field offset
    size_t fbe_offset() const noexcept { return _offset; }
    // Get the field size
    size_t fbe_size() const noexcept { return 4; }
    // Get the field body size
    size_t fbe_body() const noexcept;
    // Get the field extra size
    size_t fbe_extra() const noexcept;
    // Get the field type
    static constexpr size_t fbe_type() noexcept { return 131; }

    // Shift the current field offset
    void fbe_shift(size_t size) noexcept { _offset += size; }
    // Unshift the current field offset
    void fbe_unshift(size_t size) noexcept { _offset -= size; }

    // Check if the struct value is valid
    bool verify(bool fbe_verify_type = true) const noexcept;
    // Check if the struct fields are valid
    bool verify_fields(size_t fbe_struct_size) const noexcept;

    // Get the struct value (begin phase)
    size_t get_begin() const noexcept;
    // Get the struct value (end phase)
    void get_end(size_t fbe_begin) const noexcept;

    // Get the struct value
    void get(::test_pmr::StructList& fbe_value, pmr::memory_resource* resource) const noexcept;
    // Get the struct fields values
    void get_fields(::test_pmr::StructList& fbe_value, size_t fbe_struct_size, pmr::memory_resource* resource) const noexcept;

    // Set the struct value (begin phase)
    size_t set_begin();
    // Set the struct value (end phase)
    void set_end(size_t fbe_begin);

    // Set the struct value
    void set(const ::test_pmr::StructList& fbe_value, pmr::memory_resource* resource) noexcept;
    // Set the struct fields values
    void set_fields(const ::test_pmr::StructList& fbe_value, pmr::memory_resource* resource) noexcept;

private:
    FBEBuffer& _buffer;
    size_t _offset;

public:
    FieldModelVector<uint8_t> f1;
    FieldModelVector<std::optional<uint8_t>> f2;
    FieldModelVector<FBE::pmr_buffer_t> f3;
    FieldModelVector<std::optional<FBE::pmr_buffer_t>> f4;
    FieldModelVector<::test_pmr::EnumSimple> f5;
    FieldModelVector<std::optional<::test_pmr::EnumSimple>> f6;
    FieldModelVector<::test_pmr::FlagsSimple> f7;
    FieldModelVector<std::optional<::test_pmr::FlagsSimple>> f8;
    FieldModelVector<::test_pmr::StructSimple> f9;
    FieldModelVector<std::optional<::test_pmr::StructSimple>> f10;
};

namespace test_pmr {

// Fast Binary Encoding StructList model
class StructListModel : public FBE::Model
{
public:
    StructListModel() : model(this->buffer(), 4) {}
    StructListModel(const std::shared_ptr<FBEBuffer>& buffer) : FBE::Model(buffer), model(this->buffer(), 4) {}

    // Get the model size
    size_t fbe_size() const noexcept { return model.fbe_size() + model.fbe_extra(); }
    // Get the model type
    static constexpr size_t fbe_type() noexcept { return FieldModel<::test_pmr::StructList>::fbe_type(); }

    // Check if the struct value is valid
    bool verify();

    // Create a new model (begin phase)
    size_t create_begin();
    // Create a new model (end phase)
    size_t create_end(size_t fbe_begin);

    // Serialize the struct value
    size_t serialize(const ::test_pmr::StructList& value, pmr::memory_resource* resource);
    // Deserialize the struct value
    size_t deserialize(::test_pmr::StructList& value, pmr::memory_resource* resource) const noexcept;

    // Move to the next struct value
    void next(size_t prev) noexcept { model.fbe_shift(prev); }

public:
    FieldModel<::test_pmr::StructList> model;
};

} // namespace test_pmr

template<>
struct model_of<::test_pmr::StructList> {
using type = test_pmr::StructListModel;
};


// Fast Binary Encoding ::test_pmr::StructSet field model
template <>
class FieldModel<::test_pmr::StructSet>
{
public:
    FieldModel(FBEBuffer& buffer, size_t offset) noexcept;

    // Get the field offset
    size_t fbe_offset() const noexcept { return _offset; }
    // Get the field size
    size_t fbe_size() const noexcept { return 4; }
    // Get the field body size
    size_t fbe_body() const noexcept;
    // Get the field extra size
    size_t fbe_extra() const noexcept;
    // Get the field type
    static constexpr size_t fbe_type() noexcept { return 132; }

    // Shift the current field offset
    void fbe_shift(size_t size) noexcept { _offset += size; }
    // Unshift the current field offset
    void fbe_unshift(size_t size) noexcept { _offset -= size; }

    // Check if the struct value is valid
    bool verify(bool fbe_verify_type = true) const noexcept;
    // Check if the struct fields are valid
    bool verify_fields(size_t fbe_struct_size) const noexcept;

    // Get the struct value (begin phase)
    size_t get_begin() const noexcept;
    // Get the struct value (end phase)
    void get_end(size_t fbe_begin) const noexcept;

    // Get the struct value
    void get(::test_pmr::StructSet& fbe_value, pmr::memory_resource* resource) const noexcept;
    // Get the struct fields values
    void get_fields(::test_pmr::StructSet& fbe_value, size_t fbe_struct_size, pmr::memory_resource* resource) const noexcept;

    // Set the struct value (begin phase)
    size_t set_begin();
    // Set the struct value (end phase)
    void set_end(size_t fbe_begin);

    // Set the struct value
    void set(const ::test_pmr::StructSet& fbe_value, pmr::memory_resource* resource) noexcept;
    // Set the struct fields values
    void set_fields(const ::test_pmr::StructSet& fbe_value, pmr::memory_resource* resource) noexcept;

private:
    FBEBuffer& _buffer;
    size_t _offset;

public:
    FieldModelVector<uint8_t> f1;
    FieldModelVector<::test_pmr::EnumSimple> f2;
    FieldModelVector<::test_pmr::FlagsSimple> f3;
    FieldModelVector<::test_pmr::StructSimple> f4;
};

namespace test_pmr {

// Fast Binary Encoding StructSet model
class StructSetModel : public FBE::Model
{
public:
    StructSetModel() : model(this->buffer(), 4) {}
    StructSetModel(const std::shared_ptr<FBEBuffer>& buffer) : FBE::Model(buffer), model(this->buffer(), 4) {}

    // Get the model size
    size_t fbe_size() const noexcept { return model.fbe_size() + model.fbe_extra(); }
    // Get the model type
    static constexpr size_t fbe_type() noexcept { return FieldModel<::test_pmr::StructSet>::fbe_type(); }

    // Check if the struct value is valid
    bool verify();

    // Create a new model (begin phase)
    size_t create_begin();
    // Create a new model (end phase)
    size_t create_end(size_t fbe_begin);

    // Serialize the struct value
    size_t serialize(const ::test_pmr::StructSet& value, pmr::memory_resource* resource);
    // Deserialize the struct value
    size_t deserialize(::test_pmr::StructSet& value, pmr::memory_resource* resource) const noexcept;

    // Move to the next struct value
    void next(size_t prev) noexcept { model.fbe_shift(prev); }

public:
    FieldModel<::test_pmr::StructSet> model;
};

} // namespace test_pmr

template<>
struct model_of<::test_pmr::StructSet> {
using type = test_pmr::StructSetModel;
};


// Fast Binary Encoding ::test_pmr::StructMap field model
template <>
class FieldModel<::test_pmr::StructMap>
{
public:
    FieldModel(FBEBuffer& buffer, size_t offset) noexcept;

    // Get the field offset
    size_t fbe_offset() const noexcept { return _offset; }
    // Get the field size
    size_t fbe_size() const noexcept { return 4; }
    // Get the field body size
    size_t fbe_body() const noexcept;
    // Get the field extra size
    size_t fbe_extra() const noexcept;
    // Get the field type
    static constexpr size_t fbe_type() noexcept { return 140; }

    // Shift the current field offset
    void fbe_shift(size_t size) noexcept { _offset += size; }
    // Unshift the current field offset
    void fbe_unshift(size_t size) noexcept { _offset -= size; }

    // Check if the struct value is valid
    bool verify(bool fbe_verify_type = true) const noexcept;
    // Check if the struct fields are valid
    bool verify_fields(size_t fbe_struct_size) const noexcept;

    // Get the struct value (begin phase)
    size_t get_begin() const noexcept;
    // Get the struct value (end phase)
    void get_end(size_t fbe_begin) const noexcept;

    // Get the struct value
    void get(::test_pmr::StructMap& fbe_value, pmr::memory_resource* resource) const noexcept;
    // Get the struct fields values
    void get_fields(::test_pmr::StructMap& fbe_value, size_t fbe_struct_size, pmr::memory_resource* resource) const noexcept;

    // Set the struct value (begin phase)
    size_t set_begin();
    // Set the struct value (end phase)
    void set_end(size_t fbe_begin);

    // Set the struct value
    void set(const ::test_pmr::StructMap& fbe_value, pmr::memory_resource* resource) noexcept;
    // Set the struct fields values
    void set_fields(const ::test_pmr::StructMap& fbe_value, pmr::memory_resource* resource) noexcept;

private:
    FBEBuffer& _buffer;
    size_t _offset;

public:
    FieldModelMap<int32_t, uint8_t> f1;
    FieldModelMap<int32_t, std::optional<uint8_t>> f2;
    FieldModelMap<int32_t, FBE::pmr_buffer_t> f3;
    FieldModelMap<int32_t, std::optional<FBE::pmr_buffer_t>> f4;
    FieldModelMap<int32_t, ::test_pmr::EnumSimple> f5;
    FieldModelMap<int32_t, std::optional<::test_pmr::EnumSimple>> f6;
    FieldModelMap<int32_t, ::test_pmr::FlagsSimple> f7;
    FieldModelMap<int32_t, std::optional<::test_pmr::FlagsSimple>> f8;
    FieldModelMap<int32_t, ::test_pmr::StructSimple> f9;
    FieldModelMap<int32_t, std::optional<::test_pmr::StructSimple>> f10;
};

namespace test_pmr {

// Fast Binary Encoding StructMap model
class StructMapModel : public FBE::Model
{
public:
    StructMapModel() : model(this->buffer(), 4) {}
    StructMapModel(const std::shared_ptr<FBEBuffer>& buffer) : FBE::Model(buffer), model(this->buffer(), 4) {}

    // Get the model size
    size_t fbe_size() const noexcept { return model.fbe_size() + model.fbe_extra(); }
    // Get the model type
    static constexpr size_t fbe_type() noexcept { return FieldModel<::test_pmr::StructMap>::fbe_type(); }

    // Check if the struct value is valid
    bool verify();

    // Create a new model (begin phase)
    size_t create_begin();
    // Create a new model (end phase)
    size_t create_end(size_t fbe_begin);

    // Serialize the struct value
    size_t serialize(const ::test_pmr::StructMap& value, pmr::memory_resource* resource);
    // Deserialize the struct value
    size_t deserialize(::test_pmr::StructMap& value, pmr::memory_resource* resource) const noexcept;

    // Move to the next struct value
    void next(size_t prev) noexcept { model.fbe_shift(prev); }

public:
    FieldModel<::test_pmr::StructMap> model;
};

} // namespace test_pmr

template<>
struct model_of<::test_pmr::StructMap> {
using type = test_pmr::StructMapModel;
};


// Fast Binary Encoding ::test_pmr::StructHash field model
template <>
class FieldModel<::test_pmr::StructHash>
{
public:
    FieldModel(FBEBuffer& buffer, size_t offset) noexcept;

    // Get the field offset
    size_t fbe_offset() const noexcept { return _offset; }
    // Get the field size
    size_t fbe_size() const noexcept { return 4; }
    // Get the field body size
    size_t fbe_body() const noexcept;
    // Get the field extra size
    size_t fbe_extra() const noexcept;
    // Get the field type
    static constexpr size_t fbe_type() noexcept { return 141; }

    // Shift the current field offset
    void fbe_shift(size_t size) noexcept { _offset += size; }
    // Unshift the current field offset
    void fbe_unshift(size_t size) noexcept { _offset -= size; }

    // Check if the struct value is valid
    bool verify(bool fbe_verify_type = true) const noexcept;
    // Check if the struct fields are valid
    bool verify_fields(size_t fbe_struct_size) const noexcept;

    // Get the struct value (begin phase)
    size_t get_begin() const noexcept;
    // Get the struct value (end phase)
    void get_end(size_t fbe_begin) const noexcept;

    // Get the struct value
    void get(::test_pmr::StructHash& fbe_value, pmr::memory_resource* resource) const noexcept;
    // Get the struct fields values
    void get_fields(::test_pmr::StructHash& fbe_value, size_t fbe_struct_size, pmr::memory_resource* resource) const noexcept;

    // Set the struct value (begin phase)
    size_t set_begin();
    // Set the struct value (end phase)
    void set_end(size_t fbe_begin);

    // Set the struct value
    void set(const ::test_pmr::StructHash& fbe_value, pmr::memory_resource* resource) noexcept;
    // Set the struct fields values
    void set_fields(const ::test_pmr::StructHash& fbe_value, pmr::memory_resource* resource) noexcept;

private:
    FBEBuffer& _buffer;
    size_t _offset;

public:
    FieldModelMap<ArenaString, uint8_t> f1;
    FieldModelMap<ArenaString, std::optional<uint8_t>> f2;
    FieldModelMap<ArenaString, FBE::pmr_buffer_t> f3;
    FieldModelMap<ArenaString, std::optional<FBE::pmr_buffer_t>> f4;
    FieldModelMap<ArenaString, ::test_pmr::EnumSimple> f5;
    FieldModelMap<ArenaString, std::optional<::test_pmr::EnumSimple>> f6;
    FieldModelMap<ArenaString, ::test_pmr::FlagsSimple> f7;
    FieldModelMap<ArenaString, std::optional<::test_pmr::FlagsSimple>> f8;
    FieldModelMap<ArenaString, ::test_pmr::StructSimple> f9;
    FieldModelMap<ArenaString, std::optional<::test_pmr::StructSimple>> f10;
};

namespace test_pmr {

// Fast Binary Encoding StructHash model
class StructHashModel : public FBE::Model
{
public:
    StructHashModel() : model(this->buffer(), 4) {}
    StructHashModel(const std::shared_ptr<FBEBuffer>& buffer) : FBE::Model(buffer), model(this->buffer(), 4) {}

    // Get the model size
    size_t fbe_size() const noexcept { return model.fbe_size() + model.fbe_extra(); }
    // Get the model type
    static constexpr size_t fbe_type() noexcept { return FieldModel<::test_pmr::StructHash>::fbe_type(); }

    // Check if the struct value is valid
    bool verify();

    // Create a new model (begin phase)
    size_t create_begin();
    // Create a new model (end phase)
    size_t create_end(size_t fbe_begin);

    // Serialize the struct value
    size_t serialize(const ::test_pmr::StructHash& value, pmr::memory_resource* resource);
    // Deserialize the struct value
    size_t deserialize(::test_pmr::StructHash& value, pmr::memory_resource* resource) const noexcept;

    // Move to the next struct value
    void next(size_t prev) noexcept { model.fbe_shift(prev); }

public:
    FieldModel<::test_pmr::StructHash> model;
};

} // namespace test_pmr

template<>
struct model_of<::test_pmr::StructHash> {
using type = test_pmr::StructHashModel;
};


// Fast Binary Encoding ::test_pmr::StructHashEx field model
template <>
class FieldModel<::test_pmr::StructHashEx>
{
public:
    FieldModel(FBEBuffer& buffer, size_t offset) noexcept;

    // Get the field offset
    size_t fbe_offset() const noexcept { return _offset; }
    // Get the field size
    size_t fbe_size() const noexcept { return 4; }
    // Get the field body size
    size_t fbe_body() const noexcept;
    // Get the field extra size
    size_t fbe_extra() const noexcept;
    // Get the field type
    static constexpr size_t fbe_type() noexcept { return 142; }

    // Shift the current field offset
    void fbe_shift(size_t size) noexcept { _offset += size; }
    // Unshift the current field offset
    void fbe_unshift(size_t size) noexcept { _offset -= size; }

    // Check if the struct value is valid
    bool verify(bool fbe_verify_type = true) const noexcept;
    // Check if the struct fields are valid
    bool verify_fields(size_t fbe_struct_size) const noexcept;

    // Get the struct value (begin phase)
    size_t get_begin() const noexcept;
    // Get the struct value (end phase)
    void get_end(size_t fbe_begin) const noexcept;

    // Get the struct value
    void get(::test_pmr::StructHashEx& fbe_value, pmr::memory_resource* resource) const noexcept;
    // Get the struct fields values
    void get_fields(::test_pmr::StructHashEx& fbe_value, size_t fbe_struct_size, pmr::memory_resource* resource) const noexcept;

    // Set the struct value (begin phase)
    size_t set_begin();
    // Set the struct value (end phase)
    void set_end(size_t fbe_begin);

    // Set the struct value
    void set(const ::test_pmr::StructHashEx& fbe_value, pmr::memory_resource* resource) noexcept;
    // Set the struct fields values
    void set_fields(const ::test_pmr::StructHashEx& fbe_value, pmr::memory_resource* resource) noexcept;

private:
    FBEBuffer& _buffer;
    size_t _offset;

public:
    FieldModelMap<::test_pmr::StructSimple, ::test_pmr::StructNested> f1;
    FieldModelMap<::test_pmr::StructSimple, std::optional<::test_pmr::StructNested>> f2;
};

namespace test_pmr {

// Fast Binary Encoding StructHashEx model
class StructHashExModel : public FBE::Model
{
public:
    StructHashExModel() : model(this->buffer(), 4) {}
    StructHashExModel(const std::shared_ptr<FBEBuffer>& buffer) : FBE::Model(buffer), model(this->buffer(), 4) {}

    // Get the model size
    size_t fbe_size() const noexcept { return model.fbe_size() + model.fbe_extra(); }
    // Get the model type
    static constexpr size_t fbe_type() noexcept { return FieldModel<::test_pmr::StructHashEx>::fbe_type(); }

    // Check if the struct value is valid
    bool verify();

    // Create a new model (begin phase)
    size_t create_begin();
    // Create a new model (end phase)
    size_t create_end(size_t fbe_begin);

    // Serialize the struct value
    size_t serialize(const ::test_pmr::StructHashEx& value, pmr::memory_resource* resource);
    // Deserialize the struct value
    size_t deserialize(::test_pmr::StructHashEx& value, pmr::memory_resource* resource) const noexcept;

    // Move to the next struct value
    void next(size_t prev) noexcept { model.fbe_shift(prev); }

public:
    FieldModel<::test_pmr::StructHashEx> model;
};

} // namespace test_pmr

template<>
struct model_of<::test_pmr::StructHashEx> {
using type = test_pmr::StructHashExModel;
};


// Fast Binary Encoding ::test_pmr::StructEmpty field model
template <>
class FieldModel<::test_pmr::StructEmpty>
{
public:
    FieldModel(FBEBuffer& buffer, size_t offset) noexcept;

    // Get the field offset
    size_t fbe_offset() const noexcept { return _offset; }
    // Get the field size
    size_t fbe_size() const noexcept { return 4; }
    // Get the field body size
    size_t fbe_body() const noexcept;
    // Get the field extra size
    size_t fbe_extra() const noexcept;
    // Get the field type
    static constexpr size_t fbe_type() noexcept { return 143; }

    // Shift the current field offset
    void fbe_shift(size_t size) noexcept { _offset += size; }
    // Unshift the current field offset
    void fbe_unshift(size_t size) noexcept { _offset -= size; }

    // Check if the struct value is valid
    bool verify(bool fbe_verify_type = true) const noexcept;
    // Check if the struct fields are valid
    bool verify_fields(size_t fbe_struct_size) const noexcept;

    // Get the struct value (begin phase)
    size_t get_begin() const noexcept;
    // Get the struct value (end phase)
    void get_end(size_t fbe_begin) const noexcept;

    // Get the struct value
    void get(::test_pmr::StructEmpty& fbe_value, pmr::memory_resource* resource) const noexcept;
    // Get the struct fields values
    void get_fields(::test_pmr::StructEmpty& fbe_value, size_t fbe_struct_size, pmr::memory_resource* resource) const noexcept;

    // Set the struct value (begin phase)
    size_t set_begin();
    // Set the struct value (end phase)
    void set_end(size_t fbe_begin);

    // Set the struct value
    void set(const ::test_pmr::StructEmpty& fbe_value, pmr::memory_resource* resource) noexcept;
    // Set the struct fields values
    void set_fields(const ::test_pmr::StructEmpty& fbe_value, pmr::memory_resource* resource) noexcept;

private:
    FBEBuffer& _buffer;
    size_t _offset;

public:
};

namespace test_pmr {

// Fast Binary Encoding StructEmpty model
class StructEmptyModel : public FBE::Model
{
public:
    StructEmptyModel() : model(this->buffer(), 4) {}
    StructEmptyModel(const std::shared_ptr<FBEBuffer>& buffer) : FBE::Model(buffer), model(this->buffer(), 4) {}

    // Get the model size
    size_t fbe_size() const noexcept { return model.fbe_size() + model.fbe_extra(); }
    // Get the model type
    static constexpr size_t fbe_type() noexcept { return FieldModel<::test_pmr::StructEmpty>::fbe_type(); }

    // Check if the struct value is valid
    bool verify();

    // Create a new model (begin phase)
    size_t create_begin();
    // Create a new model (end phase)
    size_t create_end(size_t fbe_begin);

    // Serialize the struct value
    size_t serialize(const ::test_pmr::StructEmpty& value, pmr::memory_resource* resource);
    // Deserialize the struct value
    size_t deserialize(::test_pmr::StructEmpty& value, pmr::memory_resource* resource) const noexcept;

    // Move to the next struct value
    void next(size_t prev) noexcept { model.fbe_shift(prev); }

public:
    FieldModel<::test_pmr::StructEmpty> model;
};

} // namespace test_pmr

template<>
struct model_of<::test_pmr::StructEmpty> {
using type = test_pmr::StructEmptyModel;
};


// Fast Binary Encoding ::test_pmr::StructFieldNamedString field model
template <>
class FieldModel<::test_pmr::StructFieldNamedString>
{
public:
    FieldModel(FBEBuffer& buffer, size_t offset) noexcept;

    // Get the field offset
    size_t fbe_offset() const noexcept { return _offset; }
    // Get the field size
    size_t fbe_size() const noexcept { return 4; }
    // Get the field body size
    size_t fbe_body() const noexcept;
    // Get the field extra size
    size_t fbe_extra() const noexcept;
    // Get the field type
    static constexpr size_t fbe_type() noexcept { return 144; }

    // Shift the current field offset
    void fbe_shift(size_t size) noexcept { _offset += size; }
    // Unshift the current field offset
    void fbe_unshift(size_t size) noexcept { _offset -= size; }

    // Check if the struct value is valid
    bool verify(bool fbe_verify_type = true) const noexcept;
    // Check if the struct fields are valid
    bool verify_fields(size_t fbe_struct_size) const noexcept;

    // Get the struct value (begin phase)
    size_t get_begin() const noexcept;
    // Get the struct value (end phase)
    void get_end(size_t fbe_begin) const noexcept;

    // Get the struct value
    void get(::test_pmr::StructFieldNamedString& fbe_value, pmr::memory_resource* resource) const noexcept;
    // Get the struct fields values
    void get_fields(::test_pmr::StructFieldNamedString& fbe_value, size_t fbe_struct_size, pmr::memory_resource* resource) const noexcept;

    // Set the struct value (begin phase)
    size_t set_begin();
    // Set the struct value (end phase)
    void set_end(size_t fbe_begin);

    // Set the struct value
    void set(const ::test_pmr::StructFieldNamedString& fbe_value, pmr::memory_resource* resource) noexcept;
    // Set the struct fields values
    void set_fields(const ::test_pmr::StructFieldNamedString& fbe_value, pmr::memory_resource* resource) noexcept;

private:
    FBEBuffer& _buffer;
    size_t _offset;

public:
    FieldModel<::test_pmr::StructEmpty> string;
};

namespace test_pmr {

// Fast Binary Encoding StructFieldNamedString model
class StructFieldNamedStringModel : public FBE::Model
{
public:
    StructFieldNamedStringModel() : model(this->buffer(), 4) {}
    StructFieldNamedStringModel(const std::shared_ptr<FBEBuffer>& buffer) : FBE::Model(buffer), model(this->buffer(), 4) {}

    // Get the model size
    size_t fbe_size() const noexcept { return model.fbe_size() + model.fbe_extra(); }
    // Get the model type
    static constexpr size_t fbe_type() noexcept { return FieldModel<::test_pmr::StructFieldNamedString>::fbe_type(); }

    // Check if the struct value is valid
    bool verify();

    // Create a new model (begin phase)
    size_t create_begin();
    // Create a new model (end phase)
    size_t create_end(size_t fbe_begin);

    // Serialize the struct value
    size_t serialize(const ::test_pmr::StructFieldNamedString& value, pmr::memory_resource* resource);
    // Deserialize the struct value
    size_t deserialize(::test_pmr::StructFieldNamedString& value, pmr::memory_resource* resource) const noexcept;

    // Move to the next struct value
    void next(size_t prev) noexcept { model.fbe_shift(prev); }

public:
    FieldModel<::test_pmr::StructFieldNamedString> model;
};

} // namespace test_pmr

template<>
struct model_of<::test_pmr::StructFieldNamedString> {
using type = test_pmr::StructFieldNamedStringModel;
};


// Fast Binary Encoding ::test_pmr::StringFieldNamedString field model
template <>
class FieldModel<::test_pmr::StringFieldNamedString>
{
public:
    FieldModel(FBEBuffer& buffer, size_t offset) noexcept;

    // Get the field offset
    size_t fbe_offset() const noexcept { return _offset; }
    // Get the field size
    size_t fbe_size() const noexcept { return 4; }
    // Get the field body size
    size_t fbe_body() const noexcept;
    // Get the field extra size
    size_t fbe_extra() const noexcept;
    // Get the field type
    static constexpr size_t fbe_type() noexcept { return 145; }

    // Shift the current field offset
    void fbe_shift(size_t size) noexcept { _offset += size; }
    // Unshift the current field offset
    void fbe_unshift(size_t size) noexcept { _offset -= size; }

    // Check if the struct value is valid
    bool verify(bool fbe_verify_type = true) const noexcept;
    // Check if the struct fields are valid
    bool verify_fields(size_t fbe_struct_size) const noexcept;

    // Get the struct value (begin phase)
    size_t get_begin() const noexcept;
    // Get the struct value (end phase)
    void get_end(size_t fbe_begin) const noexcept;

    // Get the struct value
    void get(::test_pmr::StringFieldNamedString& fbe_value, pmr::memory_resource* resource) const noexcept;
    // Get the struct fields values
    void get_fields(::test_pmr::StringFieldNamedString& fbe_value, size_t fbe_struct_size, pmr::memory_resource* resource) const noexcept;

    // Set the struct value (begin phase)
    size_t set_begin();
    // Set the struct value (end phase)
    void set_end(size_t fbe_begin);

    // Set the struct value
    void set(const ::test_pmr::StringFieldNamedString& fbe_value, pmr::memory_resource* resource) noexcept;
    // Set the struct fields values
    void set_fields(const ::test_pmr::StringFieldNamedString& fbe_value, pmr::memory_resource* resource) noexcept;

private:
    FBEBuffer& _buffer;
    size_t _offset;

public:
    FieldModel<ArenaString> string;
};

namespace test_pmr {

// Fast Binary Encoding StringFieldNamedString model
class StringFieldNamedStringModel : public FBE::Model
{
public:
    StringFieldNamedStringModel() : model(this->buffer(), 4) {}
    StringFieldNamedStringModel(const std::shared_ptr<FBEBuffer>& buffer) : FBE::Model(buffer), model(this->buffer(), 4) {}

    // Get the model size
    size_t fbe_size() const noexcept { return model.fbe_size() + model.fbe_extra(); }
    // Get the model type
    static constexpr size_t fbe_type() noexcept { return FieldModel<::test_pmr::StringFieldNamedString>::fbe_type(); }

    // Check if the struct value is valid
    bool verify();

    // Create a new model (begin phase)
    size_t create_begin();
    // Create a new model (end phase)
    size_t create_end(size_t fbe_begin);

    // Serialize the struct value
    size_t serialize(const ::test_pmr::StringFieldNamedString& value, pmr::memory_resource* resource);
    // Deserialize the struct value
    size_t deserialize(::test_pmr::StringFieldNamedString& value, pmr::memory_resource* resource) const noexcept;

    // Move to the next struct value
    void next(size_t prev) noexcept { model.fbe_shift(prev); }

public:
    FieldModel<::test_pmr::StringFieldNamedString> model;
};

} // namespace test_pmr

template<>
struct model_of<::test_pmr::StringFieldNamedString> {
using type = test_pmr::StringFieldNamedStringModel;
};


// Fast Binary Encoding ::test_pmr::OptionalFieldNamedString field model
template <>
class FieldModel<::test_pmr::OptionalFieldNamedString>
{
public:
    FieldModel(FBEBuffer& buffer, size_t offset) noexcept;

    // Get the field offset
    size_t fbe_offset() const noexcept { return _offset; }
    // Get the field size
    size_t fbe_size() const noexcept { return 4; }
    // Get the field body size
    size_t fbe_body() const noexcept;
    // Get the field extra size
    size_t fbe_extra() const noexcept;
    // Get the field type
    static constexpr size_t fbe_type() noexcept { return 146; }

    // Shift the current field offset
    void fbe_shift(size_t size) noexcept { _offset += size; }
    // Unshift the current field offset
    void fbe_unshift(size_t size) noexcept { _offset -= size; }

    // Check if the struct value is valid
    bool verify(bool fbe_verify_type = true) const noexcept;
    // Check if the struct fields are valid
    bool verify_fields(size_t fbe_struct_size) const noexcept;

    // Get the struct value (begin phase)
    size_t get_begin() const noexcept;
    // Get the struct value (end phase)
    void get_end(size_t fbe_begin) const noexcept;

    // Get the struct value
    void get(::test_pmr::OptionalFieldNamedString& fbe_value, pmr::memory_resource* resource) const noexcept;
    // Get the struct fields values
    void get_fields(::test_pmr::OptionalFieldNamedString& fbe_value, size_t fbe_struct_size, pmr::memory_resource* resource) const noexcept;

    // Set the struct value (begin phase)
    size_t set_begin();
    // Set the struct value (end phase)
    void set_end(size_t fbe_begin);

    // Set the struct value
    void set(const ::test_pmr::OptionalFieldNamedString& fbe_value, pmr::memory_resource* resource) noexcept;
    // Set the struct fields values
    void set_fields(const ::test_pmr::OptionalFieldNamedString& fbe_value, pmr::memory_resource* resource) noexcept;

private:
    FBEBuffer& _buffer;
    size_t _offset;

public:
    FieldModel<std::optional<ArenaString>> string;
};

namespace test_pmr {

// Fast Binary Encoding OptionalFieldNamedString model
class OptionalFieldNamedStringModel : public FBE::Model
{
public:
    OptionalFieldNamedStringModel() : model(this->buffer(), 4) {}
    OptionalFieldNamedStringModel(const std::shared_ptr<FBEBuffer>& buffer) : FBE::Model(buffer), model(this->buffer(), 4) {}

    // Get the model size
    size_t fbe_size() const noexcept { return model.fbe_size() + model.fbe_extra(); }
    // Get the model type
    static constexpr size_t fbe_type() noexcept { return FieldModel<::test_pmr::OptionalFieldNamedString>::fbe_type(); }

    // Check if the struct value is valid
    bool verify();

    // Create a new model (begin phase)
    size_t create_begin();
    // Create a new model (end phase)
    size_t create_end(size_t fbe_begin);

    // Serialize the struct value
    size_t serialize(const ::test_pmr::OptionalFieldNamedString& value, pmr::memory_resource* resource);
    // Deserialize the struct value
    size_t deserialize(::test_pmr::OptionalFieldNamedString& value, pmr::memory_resource* resource) const noexcept;

    // Move to the next struct value
    void next(size_t prev) noexcept { model.fbe_shift(prev); }

public:
    FieldModel<::test_pmr::OptionalFieldNamedString> model;
};

} // namespace test_pmr

template<>
struct model_of<::test_pmr::OptionalFieldNamedString> {
using type = test_pmr::OptionalFieldNamedStringModel;
};


// Fast Binary Encoding ::test_pmr::ArrayFieldNamedString field model
template <>
class FieldModel<::test_pmr::ArrayFieldNamedString>
{
public:
    FieldModel(FBEBuffer& buffer, size_t offset) noexcept;

    // Get the field offset
    size_t fbe_offset() const noexcept { return _offset; }
    // Get the field size
    size_t fbe_size() const noexcept { return 4; }
    // Get the field body size
    size_t fbe_body() const noexcept;
    // Get the field extra size
    size_t fbe_extra() const noexcept;
    // Get the field type
    static constexpr size_t fbe_type() noexcept { return 147; }

    // Shift the current field offset
    void fbe_shift(size_t size) noexcept { _offset += size; }
    // Unshift the current field offset
    void fbe_unshift(size_t size) noexcept { _offset -= size; }

    // Check if the struct value is valid
    bool verify(bool fbe_verify_type = true) const noexcept;
    // Check if the struct fields are valid
    bool verify_fields(size_t fbe_struct_size) const noexcept;

    // Get the struct value (begin phase)
    size_t get_begin() const noexcept;
    // Get the struct value (end phase)
    void get_end(size_t fbe_begin) const noexcept;

    // Get the struct value
    void get(::test_pmr::ArrayFieldNamedString& fbe_value, pmr::memory_resource* resource) const noexcept;
    // Get the struct fields values
    void get_fields(::test_pmr::ArrayFieldNamedString& fbe_value, size_t fbe_struct_size, pmr::memory_resource* resource) const noexcept;

    // Set the struct value (begin phase)
    size_t set_begin();
    // Set the struct value (end phase)
    void set_end(size_t fbe_begin);

    // Set the struct value
    void set(const ::test_pmr::ArrayFieldNamedString& fbe_value, pmr::memory_resource* resource) noexcept;
    // Set the struct fields values
    void set_fields(const ::test_pmr::ArrayFieldNamedString& fbe_value, pmr::memory_resource* resource) noexcept;

private:
    FBEBuffer& _buffer;
    size_t _offset;

public:
    FieldModelArray<ArenaString, 4> string;
};

namespace test_pmr {

// Fast Binary Encoding ArrayFieldNamedString model
class ArrayFieldNamedStringModel : public FBE::Model
{
public:
    ArrayFieldNamedStringModel() : model(this->buffer(), 4) {}
    ArrayFieldNamedStringModel(const std::shared_ptr<FBEBuffer>& buffer) : FBE::Model(buffer), model(this->buffer(), 4) {}

    // Get the model size
    size_t fbe_size() const noexcept { return model.fbe_size() + model.fbe_extra(); }
    // Get the model type
    static constexpr size_t fbe_type() noexcept { return FieldModel<::test_pmr::ArrayFieldNamedString>::fbe_type(); }

    // Check if the struct value is valid
    bool verify();

    // Create a new model (begin phase)
    size_t create_begin();
    // Create a new model (end phase)
    size_t create_end(size_t fbe_begin);

    // Serialize the struct value
    size_t serialize(const ::test_pmr::ArrayFieldNamedString& value, pmr::memory_resource* resource);
    // Deserialize the struct value
    size_t deserialize(::test_pmr::ArrayFieldNamedString& value, pmr::memory_resource* resource) const noexcept;

    // Move to the next struct value
    void next(size_t prev) noexcept { model.fbe_shift(prev); }

public:
    FieldModel<::test_pmr::ArrayFieldNamedString> model;
};

} // namespace test_pmr

template<>
struct model_of<::test_pmr::ArrayFieldNamedString> {
using type = test_pmr::ArrayFieldNamedStringModel;
};


// Fast Binary Encoding ::test_pmr::VectorFieldNamedString field model
template <>
class FieldModel<::test_pmr::VectorFieldNamedString>
{
public:
    FieldModel(FBEBuffer& buffer, size_t offset) noexcept;

    // Get the field offset
    size_t fbe_offset() const noexcept { return _offset; }
    // Get the field size
    size_t fbe_size() const noexcept { return 4; }
    // Get the field body size
    size_t fbe_body() const noexcept;
    // Get the field extra size
    size_t fbe_extra() const noexcept;
    // Get the field type
    static constexpr size_t fbe_type() noexcept { return 148; }

    // Shift the current field offset
    void fbe_shift(size_t size) noexcept { _offset += size; }
    // Unshift the current field offset
    void fbe_unshift(size_t size) noexcept { _offset -= size; }

    // Check if the struct value is valid
    bool verify(bool fbe_verify_type = true) const noexcept;
    // Check if the struct fields are valid
    bool verify_fields(size_t fbe_struct_size) const noexcept;

    // Get the struct value (begin phase)
    size_t get_begin() const noexcept;
    // Get the struct value (end phase)
    void get_end(size_t fbe_begin) const noexcept;

    // Get the struct value
    void get(::test_pmr::VectorFieldNamedString& fbe_value, pmr::memory_resource* resource) const noexcept;
    // Get the struct fields values
    void get_fields(::test_pmr::VectorFieldNamedString& fbe_value, size_t fbe_struct_size, pmr::memory_resource* resource) const noexcept;

    // Set the struct value (begin phase)
    size_t set_begin();
    // Set the struct value (end phase)
    void set_end(size_t fbe_begin);

    // Set the struct value
    void set(const ::test_pmr::VectorFieldNamedString& fbe_value, pmr::memory_resource* resource) noexcept;
    // Set the struct fields values
    void set_fields(const ::test_pmr::VectorFieldNamedString& fbe_value, pmr::memory_resource* resource) noexcept;

private:
    FBEBuffer& _buffer;
    size_t _offset;

public:
    FieldModelVector<ArenaString> string;
};

namespace test_pmr {

// Fast Binary Encoding VectorFieldNamedString model
class VectorFieldNamedStringModel : public FBE::Model
{
public:
    VectorFieldNamedStringModel() : model(this->buffer(), 4) {}
    VectorFieldNamedStringModel(const std::shared_ptr<FBEBuffer>& buffer) : FBE::Model(buffer), model(this->buffer(), 4) {}

    // Get the model size
    size_t fbe_size() const noexcept { return model.fbe_size() + model.fbe_extra(); }
    // Get the model type
    static constexpr size_t fbe_type() noexcept { return FieldModel<::test_pmr::VectorFieldNamedString>::fbe_type(); }

    // Check if the struct value is valid
    bool verify();

    // Create a new model (begin phase)
    size_t create_begin();
    // Create a new model (end phase)
    size_t create_end(size_t fbe_begin);

    // Serialize the struct value
    size_t serialize(const ::test_pmr::VectorFieldNamedString& value, pmr::memory_resource* resource);
    // Deserialize the struct value
    size_t deserialize(::test_pmr::VectorFieldNamedString& value, pmr::memory_resource* resource) const noexcept;

    // Move to the next struct value
    void next(size_t prev) noexcept { model.fbe_shift(prev); }

public:
    FieldModel<::test_pmr::VectorFieldNamedString> model;
};

} // namespace test_pmr

template<>
struct model_of<::test_pmr::VectorFieldNamedString> {
using type = test_pmr::VectorFieldNamedStringModel;
};


// Fast Binary Encoding ::test_pmr::BytesFieldNamedString field model
template <>
class FieldModel<::test_pmr::BytesFieldNamedString>
{
public:
    FieldModel(FBEBuffer& buffer, size_t offset) noexcept;

    // Get the field offset
    size_t fbe_offset() const noexcept { return _offset; }
    // Get the field size
    size_t fbe_size() const noexcept { return 4; }
    // Get the field body size
    size_t fbe_body() const noexcept;
    // Get the field extra size
    size_t fbe_extra() const noexcept;
    // Get the field type
    static constexpr size_t fbe_type() noexcept { return 149; }

    // Shift the current field offset
    void fbe_shift(size_t size) noexcept { _offset += size; }
    // Unshift the current field offset
    void fbe_unshift(size_t size) noexcept { _offset -= size; }

    // Check if the struct value is valid
    bool verify(bool fbe_verify_type = true) const noexcept;
    // Check if the struct fields are valid
    bool verify_fields(size_t fbe_struct_size) const noexcept;

    // Get the struct value (begin phase)
    size_t get_begin() const noexcept;
    // Get the struct value (end phase)
    void get_end(size_t fbe_begin) const noexcept;

    // Get the struct value
    void get(::test_pmr::BytesFieldNamedString& fbe_value, pmr::memory_resource* resource) const noexcept;
    // Get the struct fields values
    void get_fields(::test_pmr::BytesFieldNamedString& fbe_value, size_t fbe_struct_size, pmr::memory_resource* resource) const noexcept;

    // Set the struct value (begin phase)
    size_t set_begin();
    // Set the struct value (end phase)
    void set_end(size_t fbe_begin);

    // Set the struct value
    void set(const ::test_pmr::BytesFieldNamedString& fbe_value, pmr::memory_resource* resource) noexcept;
    // Set the struct fields values
    void set_fields(const ::test_pmr::BytesFieldNamedString& fbe_value, pmr::memory_resource* resource) noexcept;

private:
    FBEBuffer& _buffer;
    size_t _offset;

public:
    FieldModel<FBE::pmr_buffer_t> string;
};

namespace test_pmr {

// Fast Binary Encoding BytesFieldNamedString model
class BytesFieldNamedStringModel : public FBE::Model
{
public:
    BytesFieldNamedStringModel() : model(this->buffer(), 4) {}
    BytesFieldNamedStringModel(const std::shared_ptr<FBEBuffer>& buffer) : FBE::Model(buffer), model(this->buffer(), 4) {}

    // Get the model size
    size_t fbe_size() const noexcept { return model.fbe_size() + model.fbe_extra(); }
    // Get the model type
    static constexpr size_t fbe_type() noexcept { return FieldModel<::test_pmr::BytesFieldNamedString>::fbe_type(); }

    // Check if the struct value is valid
    bool verify();

    // Create a new model (begin phase)
    size_t create_begin();
    // Create a new model (end phase)
    size_t create_end(size_t fbe_begin);

    // Serialize the struct value
    size_t serialize(const ::test_pmr::BytesFieldNamedString& value, pmr::memory_resource* resource);
    // Deserialize the struct value
    size_t deserialize(::test_pmr::BytesFieldNamedString& value, pmr::memory_resource* resource) const noexcept;

    // Move to the next struct value
    void next(size_t prev) noexcept { model.fbe_shift(prev); }

public:
    FieldModel<::test_pmr::BytesFieldNamedString> model;
};

} // namespace test_pmr

template<>
struct model_of<::test_pmr::BytesFieldNamedString> {
using type = test_pmr::BytesFieldNamedStringModel;
};


} // namespace FBE
