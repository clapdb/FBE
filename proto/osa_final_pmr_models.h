//------------------------------------------------------------------------------
// Automatically generated by the Fast Binary Encoding compiler, do not modify!
// https://github.com/chronoxor/FastBinaryEncoding
// Source: osa.fbe
// FBE version: 1.11.0.0
//------------------------------------------------------------------------------

#pragma once

#ifdef isset
#undef isset
#endif

#if defined(__clang__)
#pragma clang system_header
#elif defined(__GNUC__)
#pragma GCC system_header
#elif defined(_MSC_VER)
#pragma system_header
#endif

#include "fbe_final_models.h"

#include "osa_pmr.h"

namespace FBE {

// Fast Binary Encoding ::osa_pmr::Sex final model
template <>
class FinalModel<::osa_pmr::Sex> : public FinalModelBase<::osa_pmr::Sex, int32_t>
{
public:
    using FinalModelBase<::osa_pmr::Sex, int32_t>::FinalModelBase;
};

// Fast Binary Encoding ::osa_pmr::MyFLags final model
template <>
class FinalModel<::osa_pmr::MyFLags> : public FinalModelBase<::osa_pmr::MyFLags, int32_t>
{
public:
    using FinalModelBase<::osa_pmr::MyFLags, int32_t>::FinalModelBase;
};

// Fast Binary Encoding ::osa_pmr::Extra final model
template <>
class FinalModel<::osa_pmr::Extra>
{
public:
    FinalModel(FBEBuffer& buffer, size_t offset) noexcept;

    // Get the allocation size
    size_t fbe_allocation_size(const ::osa_pmr::Extra& fbe_value) const noexcept;
    // Get the final offset
    size_t fbe_offset() const noexcept { return _offset; }
    // Set the final offset
    size_t fbe_offset(size_t offset) const noexcept { return _offset = offset; }
    // Get the final type
    static constexpr size_t fbe_type() noexcept { return 1; }

    // Shift the current final offset
    void fbe_shift(size_t size) noexcept { _offset += size; }
    // Unshift the current final offset
    void fbe_unshift(size_t size) noexcept { _offset -= size; }

    // Check if the struct value is valid
    size_t verify() const noexcept;
    // Check if the struct fields are valid
    size_t verify_fields() const noexcept;

    // Get the struct value
    size_t get(::osa_pmr::Extra& fbe_value) const noexcept;
    // Get the struct fields values
    size_t get_fields(::osa_pmr::Extra& fbe_value) const noexcept;
    // Get the struct value with memory resource
    size_t get(::osa_pmr::Extra& fbe_value, std::pmr::memory_resource* resource) const noexcept;
    // Get the struct fields values with memory resource
    size_t get_fields(::osa_pmr::Extra& fbe_value, std::pmr::memory_resource* resource) const noexcept;

    // Set the struct value
    size_t set(const ::osa_pmr::Extra& fbe_value) noexcept;
    // Set the struct fields values
    size_t set_fields(const ::osa_pmr::Extra& fbe_value) noexcept;

private:
    FBEBuffer& _buffer;
    mutable size_t _offset;

public:
    FinalModel<ArenaString> name;
    FinalModel<ArenaString> detail;
    FinalModel<::osa_pmr::Sex> sex;
    FinalModel<::osa_pmr::MyFLags> flag;
};

namespace osa_pmr {

// Fast Binary Encoding Extra final model
class ExtraFinalModel : public FBE::Model
{
public:
    ExtraFinalModel() : _model(this->buffer(), 8) {}
    ExtraFinalModel(const std::shared_ptr<FBEBuffer>& buffer) : FBE::Model(buffer), _model(this->buffer(), 8) {}

    // Get the model type
    static constexpr size_t fbe_type() noexcept { return FinalModel<::osa_pmr::Extra>::fbe_type(); }

    // Check if the struct value is valid
    bool verify();

    // Serialize the struct value
    size_t serialize(const ::osa_pmr::Extra& value);
    // Deserialize the struct value
    size_t deserialize(::osa_pmr::Extra& value) const noexcept;
    // Deserialize the struct value with memory resource
    size_t deserialize(::osa_pmr::Extra& value, std::pmr::memory_resource* resource) const noexcept;

    // Move to the next struct value
    void next(size_t prev) noexcept { _model.fbe_shift(prev); }

private:
    FinalModel<::osa_pmr::Extra> _model;
};

} // namespace osa_pmr

// Fast Binary Encoding ::osa_pmr::Simple final model
template <>
class FinalModel<::osa_pmr::Simple>
{
public:
    FinalModel(FBEBuffer& buffer, size_t offset) noexcept;

    // Get the allocation size
    size_t fbe_allocation_size(const ::osa_pmr::Simple& fbe_value) const noexcept;
    // Get the final offset
    size_t fbe_offset() const noexcept { return _offset; }
    // Set the final offset
    size_t fbe_offset(size_t offset) const noexcept { return _offset = offset; }
    // Get the final type
    static constexpr size_t fbe_type() noexcept { return 2; }

    // Shift the current final offset
    void fbe_shift(size_t size) noexcept { _offset += size; }
    // Unshift the current final offset
    void fbe_unshift(size_t size) noexcept { _offset -= size; }

    // Check if the struct value is valid
    size_t verify() const noexcept;
    // Check if the struct fields are valid
    size_t verify_fields() const noexcept;

    // Get the struct value
    size_t get(::osa_pmr::Simple& fbe_value) const noexcept;
    // Get the struct fields values
    size_t get_fields(::osa_pmr::Simple& fbe_value) const noexcept;
    // Get the struct value with memory resource
    size_t get(::osa_pmr::Simple& fbe_value, std::pmr::memory_resource* resource) const noexcept;
    // Get the struct fields values with memory resource
    size_t get_fields(::osa_pmr::Simple& fbe_value, std::pmr::memory_resource* resource) const noexcept;

    // Set the struct value
    size_t set(const ::osa_pmr::Simple& fbe_value) noexcept;
    // Set the struct fields values
    size_t set_fields(const ::osa_pmr::Simple& fbe_value) noexcept;

private:
    FBEBuffer& _buffer;
    mutable size_t _offset;

public:
    FinalModel<ArenaString> name;
    FinalModel<int32_t> depth;
    FinalModelArray<::osa_pmr::Extra, 1> sa;
    FinalModel<::osa_pmr::Sex> sex;
};

namespace osa_pmr {

// Fast Binary Encoding Simple final model
class SimpleFinalModel : public FBE::Model
{
public:
    SimpleFinalModel() : _model(this->buffer(), 8) {}
    SimpleFinalModel(const std::shared_ptr<FBEBuffer>& buffer) : FBE::Model(buffer), _model(this->buffer(), 8) {}

    // Get the model type
    static constexpr size_t fbe_type() noexcept { return FinalModel<::osa_pmr::Simple>::fbe_type(); }

    // Check if the struct value is valid
    bool verify();

    // Serialize the struct value
    size_t serialize(const ::osa_pmr::Simple& value);
    // Deserialize the struct value
    size_t deserialize(::osa_pmr::Simple& value) const noexcept;
    // Deserialize the struct value with memory resource
    size_t deserialize(::osa_pmr::Simple& value, std::pmr::memory_resource* resource) const noexcept;

    // Move to the next struct value
    void next(size_t prev) noexcept { _model.fbe_shift(prev); }

private:
    FinalModel<::osa_pmr::Simple> _model;
};

} // namespace osa_pmr

} // namespace FBE
