//------------------------------------------------------------------------------
// Automatically generated by the Fast Binary Encoding compiler, do not modify!
// https://github.com/chronoxor/FastBinaryEncoding
// Source: cross_pkg_variant.fbe
// FBE version: 1.11.0.0
//------------------------------------------------------------------------------

#include "cross_pkg_variant_ptr_pmr.h"

namespace cross_pkg_variant_pmr {

auto is_equal(const MixedVariant& lhs, const MixedVariant& rhs) -> bool {
    if (lhs.index() != rhs.index())
        return false;
    switch (lhs.index()) {
        case 0 : {
            return true;
        }
        case 1: {
            return *std::get<1>(lhs) == *std::get<1>(rhs);
        }
        case 2: {
            return std::get<2>(lhs) == std::get<2>(rhs);
        }
        case 3: {
            return std::get<3>(lhs) == std::get<3>(rhs);
        }
        case 4: {
            return std::get<4>(lhs) == std::get<4>(rhs);
        }
        default: 
            return true;
    }
}

std::ostream& operator<<(std::ostream& stream, [[maybe_unused]] const MixedVariant& value)
{
    stream << "MixedVariant(variant|";
    [[maybe_unused]] bool first = true;
    switch (value.index()) {
        case 0:
            stream << "{empty}";
            break;
        case 1:
            stream<< "{extra.Extra*}";
            stream << "ptr of other struct: " << (std::get<1>(value) == nullptr ? "nullptr" : "true");
            if (std::get<1>(value) != nullptr)
            {
                stream << "->" << *std::get<1>(value);
            }
            break;
        case 2:
            stream<< "{extra.Info}";
            stream << std::get<2>(value);
            break;
        case 3:
            stream<< "{int32}";
            stream << std::get<3>(value);
            break;
        case 4:
            stream<< "{string}";
            stream << std::get<4>(value);
            break;
        default:
            static_assert("unreachable branch");
    }
    stream << ")";
    return stream;
}

auto is_equal(const PtrOnlyVariant& lhs, const PtrOnlyVariant& rhs) -> bool {
    if (lhs.index() != rhs.index())
        return false;
    switch (lhs.index()) {
        case 0 : {
            return true;
        }
        case 1: {
            return *std::get<1>(lhs) == *std::get<1>(rhs);
        }
        case 2: {
            return *std::get<2>(lhs) == *std::get<2>(rhs);
        }
        default: 
            return true;
    }
}

std::ostream& operator<<(std::ostream& stream, [[maybe_unused]] const PtrOnlyVariant& value)
{
    stream << "PtrOnlyVariant(variant|";
    [[maybe_unused]] bool first = true;
    switch (value.index()) {
        case 0:
            stream << "{empty}";
            break;
        case 1:
            stream<< "{extra.Extra*}";
            stream << "ptr of other struct: " << (std::get<1>(value) == nullptr ? "nullptr" : "true");
            if (std::get<1>(value) != nullptr)
            {
                stream << "->" << *std::get<1>(value);
            }
            break;
        case 2:
            stream<< "{extra.Info*}";
            stream << "ptr of other struct: " << (std::get<2>(value) == nullptr ? "nullptr" : "true");
            if (std::get<2>(value) != nullptr)
            {
                stream << "->" << *std::get<2>(value);
            }
            break;
        default:
            static_assert("unreachable branch");
    }
    stream << ")";
    return stream;
}

MixedContainer::MixedContainer()
    : v()
    , vo()
{}

MixedContainer::MixedContainer([[maybe_unused]] allocator_type alloc)
    : v()
    , vo(std::nullopt)
{}

MixedContainer::MixedContainer(::cross_pkg_variant_pmr::MixedVariant arg_v, std::optional<::cross_pkg_variant_pmr::MixedVariant> arg_vo)
    : v(std::move(arg_v))
    , vo()
{
    if (arg_vo.has_value()) {
        vo.emplace(std::move(arg_vo.value()));
        arg_vo.reset();
    }
}

MixedContainer::MixedContainer([[maybe_unused]] MixedContainer&& other) noexcept
    : v(std::move(other.v))
    , vo()
{
    if (other.vo.has_value()) {
        vo.emplace(std::move(other.vo.value()));
        other.vo.reset();
    }
}

MixedContainer::~MixedContainer()
{
}

bool MixedContainer::operator==([[maybe_unused]] const MixedContainer& other) const noexcept
{
    // compare variant v
    if (!is_equal(v, other.v))
        return false;
    // compare variant vo
    if ((vo.has_value() && !other.vo.has_value()) || (!vo.has_value() && other.vo.has_value()) || (vo.has_value() && other.vo.has_value() && !is_equal(vo.value(), other.vo.value())))
        return false;
    return true;
}

bool MixedContainer::operator<([[maybe_unused]] const MixedContainer& other) const noexcept
{
    return false;
}

MixedContainer& MixedContainer::operator=(MixedContainer&& other) noexcept
{
    if (this != &other)
    {
        v = std::move(other.v);
        if (other.vo.has_value()) {
            vo.emplace(std::move(other.vo.value()));
            other.vo.reset();
        }
    }
    return *this;
}

std::string MixedContainer::to_string() const
{
    std::stringstream ss; ss << *this; return ss.str();
}

void MixedContainer::swap([[maybe_unused]] MixedContainer& other) noexcept
{
    using std::swap;
    swap(v, other.v);
    swap(vo, other.vo);
}

std::ostream& operator<<(std::ostream& stream, [[maybe_unused]] const MixedContainer& value)
{
    stream << "MixedContainer(";
    stream << "v="; stream << value.v;
    stream << ",vo="; if (value.vo) stream << *value.vo; else stream << "null";
    stream << ")";
    return stream;
}

PtrOnlyContainer::PtrOnlyContainer()
    : v()
    , vv()
{}

PtrOnlyContainer::PtrOnlyContainer([[maybe_unused]] allocator_type alloc)
    : v()
    , vv(alloc)
{}

PtrOnlyContainer::PtrOnlyContainer(::cross_pkg_variant_pmr::PtrOnlyVariant arg_v, std::pmr::vector<::cross_pkg_variant_pmr::PtrOnlyVariant> arg_vv)
    : v(std::move(arg_v))
    , vv(std::move(arg_vv))
{}

PtrOnlyContainer::PtrOnlyContainer([[maybe_unused]] PtrOnlyContainer&& other) noexcept
    : v(std::move(other.v))
    , vv(std::move(other.vv))
{}

PtrOnlyContainer::~PtrOnlyContainer()
{
}

bool PtrOnlyContainer::operator==([[maybe_unused]] const PtrOnlyContainer& other) const noexcept
{
    // compare variant v
    if (!is_equal(v, other.v))
        return false;
    // compare container vv
    if (vv.size() != other.vv.size())
        return false;
    for (size_t i = 0; i < vv.size(); i++)
    {
        if (!is_equal(vv[i], other.vv[i]))
            return false;
    }
    return true;
}

bool PtrOnlyContainer::operator<([[maybe_unused]] const PtrOnlyContainer& other) const noexcept
{
    return false;
}

PtrOnlyContainer& PtrOnlyContainer::operator=(PtrOnlyContainer&& other) noexcept
{
    if (this != &other)
    {
        v = std::move(other.v);
        vv = std::move(other.vv);
    }
    return *this;
}

std::string PtrOnlyContainer::to_string() const
{
    std::stringstream ss; ss << *this; return ss.str();
}

void PtrOnlyContainer::swap([[maybe_unused]] PtrOnlyContainer& other) noexcept
{
    using std::swap;
    swap(v, other.v);
    swap(vv, other.vv);
}

std::ostream& operator<<(std::ostream& stream, [[maybe_unused]] const PtrOnlyContainer& value)
{
    stream << "PtrOnlyContainer(";
    stream << "v="; stream << value.v;
    {
        bool first = true;
        stream << ",vv=[" << value.vv.size() << "][";
        for ([[maybe_unused]] const auto& it : value.vv)
        {
            stream << std::string(first ? "" : ",") << it;
            first = false;
        }
        stream << "]";
    }
    stream << ")";
    return stream;
}

} // namespace cross_pkg_variant_pmr
