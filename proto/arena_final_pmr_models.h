//------------------------------------------------------------------------------
// Automatically generated by the Fast Binary Encoding compiler, do not modify!
// https://github.com/chronoxor/FastBinaryEncoding
// Source: arena.fbe
// FBE version: 1.11.0.0
//------------------------------------------------------------------------------

#pragma once

#ifdef isset
#undef isset
#endif

#if defined(__clang__)
#pragma clang system_header
#elif defined(__GNUC__)
#pragma GCC system_header
#elif defined(_MSC_VER)
#pragma system_header
#endif

#include "fbe_final_models.h"

#include "arena_pmr.h"

#include "arena_common_final_pmr_models.h"

namespace FBE {

// Fast Binary Encoding ::arena_pmr::Item final model
template <>
class FinalModel<::arena_pmr::Item>
{
public:
    FinalModel(FBEBuffer& buffer, size_t offset) noexcept;

    // Get the allocation size
    size_t fbe_allocation_size(const ::arena_pmr::Item& fbe_value) const noexcept;
    // Get the final offset
    size_t fbe_offset() const noexcept { return _offset; }
    // Set the final offset
    size_t fbe_offset(size_t offset) const noexcept { return _offset = offset; }
    // Get the final type
    static constexpr size_t fbe_type() noexcept { return 1; }

    // Shift the current final offset
    void fbe_shift(size_t size) noexcept { _offset += size; }
    // Unshift the current final offset
    void fbe_unshift(size_t size) noexcept { _offset -= size; }

    // Check if the struct value is valid
    size_t verify() const noexcept;
    // Check if the struct fields are valid
    size_t verify_fields() const noexcept;

    // Get the struct value
    size_t get(::arena_pmr::Item& fbe_value) const noexcept;
    // Get the struct fields values
    size_t get_fields(::arena_pmr::Item& fbe_value) const noexcept;
    // Get the struct value with memory resource
    size_t get(::arena_pmr::Item& fbe_value, std::pmr::memory_resource* resource) const noexcept;
    // Get the struct fields values with memory resource
    size_t get_fields(::arena_pmr::Item& fbe_value, std::pmr::memory_resource* resource) const noexcept;

    // Set the struct value
    size_t set(const ::arena_pmr::Item& fbe_value) noexcept;
    // Set the struct fields values
    size_t set_fields(const ::arena_pmr::Item& fbe_value) noexcept;

private:
    FBEBuffer& _buffer;
    mutable size_t _offset;

public:
    FinalModel<::arena_common_pmr::Optr> optr;
    FinalModel<::arena_common_pmr::Alias> alias;
    FinalModelVector<::arena_common_pmr::Expression> expressions;
    FinalModelMap<int32_t, ::arena_common_pmr::Alias> aliases_int;
};

namespace arena_pmr {

// Fast Binary Encoding Item final model
class ItemFinalModel : public FBE::Model
{
public:
    ItemFinalModel() : _model(this->buffer(), 8) {}
    ItemFinalModel(const std::shared_ptr<FBEBuffer>& buffer) : FBE::Model(buffer), _model(this->buffer(), 8) {}

    // Get the model type
    static constexpr size_t fbe_type() noexcept { return FinalModel<::arena_pmr::Item>::fbe_type(); }

    // Check if the struct value is valid
    bool verify();

    // Serialize the struct value
    size_t serialize(const ::arena_pmr::Item& value);
    // Deserialize the struct value
    size_t deserialize(::arena_pmr::Item& value) const noexcept;
    // Deserialize the struct value with memory resource
    size_t deserialize(::arena_pmr::Item& value, std::pmr::memory_resource* resource) const noexcept;

    // Move to the next struct value
    void next(size_t prev) noexcept { _model.fbe_shift(prev); }

private:
    FinalModel<::arena_pmr::Item> _model;
};

} // namespace arena_pmr

// Fast Binary Encoding ::arena_pmr::Item2 final model
template <>
class FinalModel<::arena_pmr::Item2>
{
public:
    FinalModel(FBEBuffer& buffer, size_t offset) noexcept;

    // Get the allocation size
    size_t fbe_allocation_size(const ::arena_pmr::Item2& fbe_value) const noexcept;
    // Get the final offset
    size_t fbe_offset() const noexcept { return _offset; }
    // Set the final offset
    size_t fbe_offset(size_t offset) const noexcept { return _offset = offset; }
    // Get the final type
    static constexpr size_t fbe_type() noexcept { return 2; }

    // Shift the current final offset
    void fbe_shift(size_t size) noexcept { _offset += size; }
    // Unshift the current final offset
    void fbe_unshift(size_t size) noexcept { _offset -= size; }

    // Check if the struct value is valid
    size_t verify() const noexcept;
    // Check if the struct fields are valid
    size_t verify_fields() const noexcept;

    // Get the struct value
    size_t get(::arena_pmr::Item2& fbe_value) const noexcept;
    // Get the struct fields values
    size_t get_fields(::arena_pmr::Item2& fbe_value) const noexcept;
    // Get the struct value with memory resource
    size_t get(::arena_pmr::Item2& fbe_value, std::pmr::memory_resource* resource) const noexcept;
    // Get the struct fields values with memory resource
    size_t get_fields(::arena_pmr::Item2& fbe_value, std::pmr::memory_resource* resource) const noexcept;

    // Set the struct value
    size_t set(const ::arena_pmr::Item2& fbe_value) noexcept;
    // Set the struct fields values
    size_t set_fields(const ::arena_pmr::Item2& fbe_value) noexcept;

private:
    FBEBuffer& _buffer;
    mutable size_t _offset;

public:
    FinalModel<FBE::pmr_buffer_t> bytes_v;
};

namespace arena_pmr {

// Fast Binary Encoding Item2 final model
class Item2FinalModel : public FBE::Model
{
public:
    Item2FinalModel() : _model(this->buffer(), 8) {}
    Item2FinalModel(const std::shared_ptr<FBEBuffer>& buffer) : FBE::Model(buffer), _model(this->buffer(), 8) {}

    // Get the model type
    static constexpr size_t fbe_type() noexcept { return FinalModel<::arena_pmr::Item2>::fbe_type(); }

    // Check if the struct value is valid
    bool verify();

    // Serialize the struct value
    size_t serialize(const ::arena_pmr::Item2& value);
    // Deserialize the struct value
    size_t deserialize(::arena_pmr::Item2& value) const noexcept;
    // Deserialize the struct value with memory resource
    size_t deserialize(::arena_pmr::Item2& value, std::pmr::memory_resource* resource) const noexcept;

    // Move to the next struct value
    void next(size_t prev) noexcept { _model.fbe_shift(prev); }

private:
    FinalModel<::arena_pmr::Item2> _model;
};

} // namespace arena_pmr

} // namespace FBE
