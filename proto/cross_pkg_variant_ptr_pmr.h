//------------------------------------------------------------------------------
// Automatically generated by the Fast Binary Encoding compiler, do not modify!
// https://github.com/chronoxor/FastBinaryEncoding
// Source: cross_pkg_variant.fbe
// FBE version: 1.11.0.0
//------------------------------------------------------------------------------

#pragma once

#ifdef isset
#undef isset
#endif

#if defined(__clang__)
#pragma clang system_header
#elif defined(__GNUC__)
#pragma GCC system_header
#elif defined(_MSC_VER)
#pragma system_header
#endif

#include "fbe.h"
#include "arena/arena.hpp"

#include "extra_ptr_pmr.h"

namespace cross_pkg_variant_pmr {
using namespace FBE;
using allocator_type = std::pmr::polymorphic_allocator<char>;
} // namespace cross_pkg_variant_pmr

namespace FBE {
using namespace ::cross_pkg_variant_pmr;
} // namespace FBE

#include "fbe_ptr.h"

namespace cross_pkg_variant_pmr {
// forward declaration
struct MixedContainer;
struct PtrOnlyContainer;

using MixedVariant = std::variant<std::monostate, ::extra_pmr::Extra*, ::extra_pmr::Info, int32_t, ArenaString>;
std::ostream& operator<<(std::ostream& stream, [[maybe_unused]] const MixedVariant& value);
auto is_equal(const MixedVariant& lhs, const MixedVariant& rhs) -> bool;

using PtrOnlyVariant = std::variant<std::monostate, ::extra_pmr::Extra*, ::extra_pmr::Info*>;
std::ostream& operator<<(std::ostream& stream, [[maybe_unused]] const PtrOnlyVariant& value);
auto is_equal(const PtrOnlyVariant& lhs, const PtrOnlyVariant& rhs) -> bool;

struct MixedContainer : FBE::Base
{
    ArenaManagedCreateOnlyTag;

    ::cross_pkg_variant_pmr::MixedVariant v;
    std::optional<::cross_pkg_variant_pmr::MixedVariant> vo;

    size_t fbe_type() const noexcept { return 1; }

    MixedContainer();
    explicit MixedContainer(allocator_type alloc);
    MixedContainer(::cross_pkg_variant_pmr::MixedVariant arg_v, std::optional<::cross_pkg_variant_pmr::MixedVariant> arg_vo);
    MixedContainer(const MixedContainer& other) = default;
    MixedContainer(MixedContainer&& other) noexcept;
    ~MixedContainer() override;

    MixedContainer& operator=(const MixedContainer& other) = default;
    MixedContainer& operator=(MixedContainer&& other) noexcept;

    bool operator==(const MixedContainer& other) const noexcept;
    bool operator!=(const MixedContainer& other) const noexcept { return !operator==(other); }
    bool operator<(const MixedContainer& other) const noexcept;
    bool operator<=(const MixedContainer& other) const noexcept { return operator<(other) || operator==(other); }
    bool operator>(const MixedContainer& other) const noexcept { return !operator<=(other); }
    bool operator>=(const MixedContainer& other) const noexcept { return !operator<(other); }

    std::string to_string() const;

    friend std::ostream& operator<<(std::ostream& stream, const MixedContainer& value);

    void swap(MixedContainer& other) noexcept;
    friend void swap(MixedContainer& value1, MixedContainer& value2) noexcept { value1.swap(value2); }
};

} // namespace cross_pkg_variant_pmr

template<>
struct std::hash<cross_pkg_variant_pmr::MixedContainer>
{
    typedef cross_pkg_variant_pmr::MixedContainer argument_type;
    typedef size_t result_type;

    result_type operator() ([[maybe_unused]] const argument_type& value) const
    {
        result_type result = 17;
        return result;
    }
};

namespace cross_pkg_variant_pmr {

struct PtrOnlyContainer : FBE::Base
{
    ArenaManagedCreateOnlyTag;

    ::cross_pkg_variant_pmr::PtrOnlyVariant v;
    std::pmr::vector<::cross_pkg_variant_pmr::PtrOnlyVariant> vv;

    size_t fbe_type() const noexcept { return 2; }

    PtrOnlyContainer();
    explicit PtrOnlyContainer(allocator_type alloc);
    PtrOnlyContainer(::cross_pkg_variant_pmr::PtrOnlyVariant arg_v, std::pmr::vector<::cross_pkg_variant_pmr::PtrOnlyVariant> arg_vv);
    PtrOnlyContainer(const PtrOnlyContainer& other) = default;
    PtrOnlyContainer(PtrOnlyContainer&& other) noexcept;
    ~PtrOnlyContainer() override;

    PtrOnlyContainer& operator=(const PtrOnlyContainer& other) = default;
    PtrOnlyContainer& operator=(PtrOnlyContainer&& other) noexcept;

    bool operator==(const PtrOnlyContainer& other) const noexcept;
    bool operator!=(const PtrOnlyContainer& other) const noexcept { return !operator==(other); }
    bool operator<(const PtrOnlyContainer& other) const noexcept;
    bool operator<=(const PtrOnlyContainer& other) const noexcept { return operator<(other) || operator==(other); }
    bool operator>(const PtrOnlyContainer& other) const noexcept { return !operator<=(other); }
    bool operator>=(const PtrOnlyContainer& other) const noexcept { return !operator<(other); }

    std::string to_string() const;

    friend std::ostream& operator<<(std::ostream& stream, const PtrOnlyContainer& value);

    void swap(PtrOnlyContainer& other) noexcept;
    friend void swap(PtrOnlyContainer& value1, PtrOnlyContainer& value2) noexcept { value1.swap(value2); }
};

} // namespace cross_pkg_variant_pmr

template<>
struct std::hash<cross_pkg_variant_pmr::PtrOnlyContainer>
{
    typedef cross_pkg_variant_pmr::PtrOnlyContainer argument_type;
    typedef size_t result_type;

    result_type operator() ([[maybe_unused]] const argument_type& value) const
    {
        result_type result = 17;
        return result;
    }
};

namespace cross_pkg_variant_pmr {

} // namespace cross_pkg_variant_pmr
