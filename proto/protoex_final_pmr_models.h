//------------------------------------------------------------------------------
// Automatically generated by the Fast Binary Encoding compiler, do not modify!
// https://github.com/chronoxor/FastBinaryEncoding
// Source: protoex.fbe
// FBE version: 1.11.0.0
//------------------------------------------------------------------------------

#pragma once

#ifdef isset
#undef isset
#endif

#if defined(__clang__)
#pragma clang system_header
#elif defined(__GNUC__)
#pragma GCC system_header
#elif defined(_MSC_VER)
#pragma system_header
#endif

#include "fbe_final_models.h"

#include "protoex_pmr.h"

#include "proto_final_pmr_models.h"

namespace FBE {

// Fast Binary Encoding ::protoex_pmr::OrderSide final model
template <>
class FinalModel<::protoex_pmr::OrderSide> : public FinalModelBase<::protoex_pmr::OrderSide, uint8_t>
{
public:
    using FinalModelBase<::protoex_pmr::OrderSide, uint8_t>::FinalModelBase;
};

// Fast Binary Encoding ::protoex_pmr::OrderType final model
template <>
class FinalModel<::protoex_pmr::OrderType> : public FinalModelBase<::protoex_pmr::OrderType, uint8_t>
{
public:
    using FinalModelBase<::protoex_pmr::OrderType, uint8_t>::FinalModelBase;
};

// Fast Binary Encoding ::protoex_pmr::StateEx final model
template <>
class FinalModel<::protoex_pmr::StateEx> : public FinalModelBase<::protoex_pmr::StateEx, uint8_t>
{
public:
    using FinalModelBase<::protoex_pmr::StateEx, uint8_t>::FinalModelBase;
};

// Fast Binary Encoding ::protoex_pmr::Order final model
template <>
class FinalModel<::protoex_pmr::Order>
{
public:
    FinalModel(FBEBuffer& buffer, size_t offset) noexcept;

    // Get the allocation size
    size_t fbe_allocation_size(const ::protoex_pmr::Order& fbe_value) const noexcept;
    // Get the final offset
    size_t fbe_offset() const noexcept { return _offset; }
    // Set the final offset
    size_t fbe_offset(size_t offset) const noexcept { return _offset = offset; }
    // Get the final type
    static constexpr size_t fbe_type() noexcept { return 1; }

    // Shift the current final offset
    void fbe_shift(size_t size) noexcept { _offset += size; }
    // Unshift the current final offset
    void fbe_unshift(size_t size) noexcept { _offset -= size; }

    // Check if the struct value is valid
    size_t verify() const noexcept;
    // Check if the struct fields are valid
    size_t verify_fields() const noexcept;

    // Get the struct value
    size_t get(::protoex_pmr::Order& fbe_value) const noexcept;
    // Get the struct fields values
    size_t get_fields(::protoex_pmr::Order& fbe_value) const noexcept;
    // Get the struct value with memory resource
    size_t get(::protoex_pmr::Order& fbe_value, std::pmr::memory_resource* resource) const noexcept;
    // Get the struct fields values with memory resource
    size_t get_fields(::protoex_pmr::Order& fbe_value, std::pmr::memory_resource* resource) const noexcept;

    // Set the struct value
    size_t set(const ::protoex_pmr::Order& fbe_value) noexcept;
    // Set the struct fields values
    size_t set_fields(const ::protoex_pmr::Order& fbe_value) noexcept;

private:
    FBEBuffer& _buffer;
    mutable size_t _offset;

public:
    FinalModel<int32_t> id;
    FinalModel<ArenaString> symbol;
    FinalModel<::protoex_pmr::OrderSide> side;
    FinalModel<::protoex_pmr::OrderType> type;
    FinalModel<double> price;
    FinalModel<double> volume;
    FinalModel<double> tp;
    FinalModel<double> sl;
};

namespace protoex_pmr {

// Fast Binary Encoding Order final model
class OrderFinalModel : public FBE::Model
{
public:
    OrderFinalModel() : _model(this->buffer(), 8) {}
    OrderFinalModel(const std::shared_ptr<FBEBuffer>& buffer) : FBE::Model(buffer), _model(this->buffer(), 8) {}

    // Get the model type
    static constexpr size_t fbe_type() noexcept { return FinalModel<::protoex_pmr::Order>::fbe_type(); }

    // Check if the struct value is valid
    bool verify();

    // Serialize the struct value
    size_t serialize(const ::protoex_pmr::Order& value);
    // Deserialize the struct value
    size_t deserialize(::protoex_pmr::Order& value) const noexcept;
    // Deserialize the struct value with memory resource
    size_t deserialize(::protoex_pmr::Order& value, std::pmr::memory_resource* resource) const noexcept;

    // Move to the next struct value
    void next(size_t prev) noexcept { _model.fbe_shift(prev); }

private:
    FinalModel<::protoex_pmr::Order> _model;
};

} // namespace protoex_pmr

// Fast Binary Encoding ::protoex_pmr::Balance final model
template <>
class FinalModel<::protoex_pmr::Balance>
{
public:
    FinalModel(FBEBuffer& buffer, size_t offset) noexcept;

    // Get the allocation size
    size_t fbe_allocation_size(const ::protoex_pmr::Balance& fbe_value) const noexcept;
    // Get the final offset
    size_t fbe_offset() const noexcept { return _offset; }
    // Set the final offset
    size_t fbe_offset(size_t offset) const noexcept { return _offset = offset; }
    // Get the final type
    static constexpr size_t fbe_type() noexcept { return FinalModel<::proto_pmr::Balance>::fbe_type(); }

    // Shift the current final offset
    void fbe_shift(size_t size) noexcept { _offset += size; }
    // Unshift the current final offset
    void fbe_unshift(size_t size) noexcept { _offset -= size; }

    // Check if the struct value is valid
    size_t verify() const noexcept;
    // Check if the struct fields are valid
    size_t verify_fields() const noexcept;

    // Get the struct value
    size_t get(::protoex_pmr::Balance& fbe_value) const noexcept;
    // Get the struct fields values
    size_t get_fields(::protoex_pmr::Balance& fbe_value) const noexcept;
    // Get the struct value with memory resource
    size_t get(::protoex_pmr::Balance& fbe_value, std::pmr::memory_resource* resource) const noexcept;
    // Get the struct fields values with memory resource
    size_t get_fields(::protoex_pmr::Balance& fbe_value, std::pmr::memory_resource* resource) const noexcept;

    // Set the struct value
    size_t set(const ::protoex_pmr::Balance& fbe_value) noexcept;
    // Set the struct fields values
    size_t set_fields(const ::protoex_pmr::Balance& fbe_value) noexcept;

private:
    FBEBuffer& _buffer;
    mutable size_t _offset;

public:
    FinalModel<::proto_pmr::Balance> parent;
    FinalModel<double> locked;
};

namespace protoex_pmr {

// Fast Binary Encoding Balance final model
class BalanceFinalModel : public FBE::Model
{
public:
    BalanceFinalModel() : _model(this->buffer(), 8) {}
    BalanceFinalModel(const std::shared_ptr<FBEBuffer>& buffer) : FBE::Model(buffer), _model(this->buffer(), 8) {}

    // Get the model type
    static constexpr size_t fbe_type() noexcept { return FinalModel<::protoex_pmr::Balance>::fbe_type(); }

    // Check if the struct value is valid
    bool verify();

    // Serialize the struct value
    size_t serialize(const ::protoex_pmr::Balance& value);
    // Deserialize the struct value
    size_t deserialize(::protoex_pmr::Balance& value) const noexcept;
    // Deserialize the struct value with memory resource
    size_t deserialize(::protoex_pmr::Balance& value, std::pmr::memory_resource* resource) const noexcept;

    // Move to the next struct value
    void next(size_t prev) noexcept { _model.fbe_shift(prev); }

private:
    FinalModel<::protoex_pmr::Balance> _model;
};

} // namespace protoex_pmr

// Fast Binary Encoding ::protoex_pmr::Account final model
template <>
class FinalModel<::protoex_pmr::Account>
{
public:
    FinalModel(FBEBuffer& buffer, size_t offset) noexcept;

    // Get the allocation size
    size_t fbe_allocation_size(const ::protoex_pmr::Account& fbe_value) const noexcept;
    // Get the final offset
    size_t fbe_offset() const noexcept { return _offset; }
    // Set the final offset
    size_t fbe_offset(size_t offset) const noexcept { return _offset = offset; }
    // Get the final type
    static constexpr size_t fbe_type() noexcept { return 3; }

    // Shift the current final offset
    void fbe_shift(size_t size) noexcept { _offset += size; }
    // Unshift the current final offset
    void fbe_unshift(size_t size) noexcept { _offset -= size; }

    // Check if the struct value is valid
    size_t verify() const noexcept;
    // Check if the struct fields are valid
    size_t verify_fields() const noexcept;

    // Get the struct value
    size_t get(::protoex_pmr::Account& fbe_value) const noexcept;
    // Get the struct fields values
    size_t get_fields(::protoex_pmr::Account& fbe_value) const noexcept;
    // Get the struct value with memory resource
    size_t get(::protoex_pmr::Account& fbe_value, std::pmr::memory_resource* resource) const noexcept;
    // Get the struct fields values with memory resource
    size_t get_fields(::protoex_pmr::Account& fbe_value, std::pmr::memory_resource* resource) const noexcept;

    // Set the struct value
    size_t set(const ::protoex_pmr::Account& fbe_value) noexcept;
    // Set the struct fields values
    size_t set_fields(const ::protoex_pmr::Account& fbe_value) noexcept;

private:
    FBEBuffer& _buffer;
    mutable size_t _offset;

public:
    FinalModel<int32_t> id;
    FinalModel<ArenaString> name;
    FinalModel<::protoex_pmr::StateEx> state;
    FinalModel<::protoex_pmr::Balance> wallet;
    FinalModel<std::optional<::protoex_pmr::Balance>> asset;
    FinalModelVector<::protoex_pmr::Order> orders;
};

namespace protoex_pmr {

// Fast Binary Encoding Account final model
class AccountFinalModel : public FBE::Model
{
public:
    AccountFinalModel() : _model(this->buffer(), 8) {}
    AccountFinalModel(const std::shared_ptr<FBEBuffer>& buffer) : FBE::Model(buffer), _model(this->buffer(), 8) {}

    // Get the model type
    static constexpr size_t fbe_type() noexcept { return FinalModel<::protoex_pmr::Account>::fbe_type(); }

    // Check if the struct value is valid
    bool verify();

    // Serialize the struct value
    size_t serialize(const ::protoex_pmr::Account& value);
    // Deserialize the struct value
    size_t deserialize(::protoex_pmr::Account& value) const noexcept;
    // Deserialize the struct value with memory resource
    size_t deserialize(::protoex_pmr::Account& value, std::pmr::memory_resource* resource) const noexcept;

    // Move to the next struct value
    void next(size_t prev) noexcept { _model.fbe_shift(prev); }

private:
    FinalModel<::protoex_pmr::Account> _model;
};

} // namespace protoex_pmr

// Fast Binary Encoding ::protoex_pmr::OrderMessage final model
template <>
class FinalModel<::protoex_pmr::OrderMessage>
{
public:
    FinalModel(FBEBuffer& buffer, size_t offset) noexcept;

    // Get the allocation size
    size_t fbe_allocation_size(const ::protoex_pmr::OrderMessage& fbe_value) const noexcept;
    // Get the final offset
    size_t fbe_offset() const noexcept { return _offset; }
    // Set the final offset
    size_t fbe_offset(size_t offset) const noexcept { return _offset = offset; }
    // Get the final type
    static constexpr size_t fbe_type() noexcept { return 11; }

    // Shift the current final offset
    void fbe_shift(size_t size) noexcept { _offset += size; }
    // Unshift the current final offset
    void fbe_unshift(size_t size) noexcept { _offset -= size; }

    // Check if the struct value is valid
    size_t verify() const noexcept;
    // Check if the struct fields are valid
    size_t verify_fields() const noexcept;

    // Get the struct value
    size_t get(::protoex_pmr::OrderMessage& fbe_value) const noexcept;
    // Get the struct fields values
    size_t get_fields(::protoex_pmr::OrderMessage& fbe_value) const noexcept;
    // Get the struct value with memory resource
    size_t get(::protoex_pmr::OrderMessage& fbe_value, std::pmr::memory_resource* resource) const noexcept;
    // Get the struct fields values with memory resource
    size_t get_fields(::protoex_pmr::OrderMessage& fbe_value, std::pmr::memory_resource* resource) const noexcept;

    // Set the struct value
    size_t set(const ::protoex_pmr::OrderMessage& fbe_value) noexcept;
    // Set the struct fields values
    size_t set_fields(const ::protoex_pmr::OrderMessage& fbe_value) noexcept;

private:
    FBEBuffer& _buffer;
    mutable size_t _offset;

public:
    FinalModel<::protoex_pmr::Order> body;
};

namespace protoex_pmr {

// Fast Binary Encoding OrderMessage final model
class OrderMessageFinalModel : public FBE::Model
{
public:
    OrderMessageFinalModel() : _model(this->buffer(), 8) {}
    OrderMessageFinalModel(const std::shared_ptr<FBEBuffer>& buffer) : FBE::Model(buffer), _model(this->buffer(), 8) {}

    // Get the model type
    static constexpr size_t fbe_type() noexcept { return FinalModel<::protoex_pmr::OrderMessage>::fbe_type(); }

    // Check if the struct value is valid
    bool verify();

    // Serialize the struct value
    size_t serialize(const ::protoex_pmr::OrderMessage& value);
    // Deserialize the struct value
    size_t deserialize(::protoex_pmr::OrderMessage& value) const noexcept;
    // Deserialize the struct value with memory resource
    size_t deserialize(::protoex_pmr::OrderMessage& value, std::pmr::memory_resource* resource) const noexcept;

    // Move to the next struct value
    void next(size_t prev) noexcept { _model.fbe_shift(prev); }

private:
    FinalModel<::protoex_pmr::OrderMessage> _model;
};

} // namespace protoex_pmr

// Fast Binary Encoding ::protoex_pmr::BalanceMessage final model
template <>
class FinalModel<::protoex_pmr::BalanceMessage>
{
public:
    FinalModel(FBEBuffer& buffer, size_t offset) noexcept;

    // Get the allocation size
    size_t fbe_allocation_size(const ::protoex_pmr::BalanceMessage& fbe_value) const noexcept;
    // Get the final offset
    size_t fbe_offset() const noexcept { return _offset; }
    // Set the final offset
    size_t fbe_offset(size_t offset) const noexcept { return _offset = offset; }
    // Get the final type
    static constexpr size_t fbe_type() noexcept { return 12; }

    // Shift the current final offset
    void fbe_shift(size_t size) noexcept { _offset += size; }
    // Unshift the current final offset
    void fbe_unshift(size_t size) noexcept { _offset -= size; }

    // Check if the struct value is valid
    size_t verify() const noexcept;
    // Check if the struct fields are valid
    size_t verify_fields() const noexcept;

    // Get the struct value
    size_t get(::protoex_pmr::BalanceMessage& fbe_value) const noexcept;
    // Get the struct fields values
    size_t get_fields(::protoex_pmr::BalanceMessage& fbe_value) const noexcept;
    // Get the struct value with memory resource
    size_t get(::protoex_pmr::BalanceMessage& fbe_value, std::pmr::memory_resource* resource) const noexcept;
    // Get the struct fields values with memory resource
    size_t get_fields(::protoex_pmr::BalanceMessage& fbe_value, std::pmr::memory_resource* resource) const noexcept;

    // Set the struct value
    size_t set(const ::protoex_pmr::BalanceMessage& fbe_value) noexcept;
    // Set the struct fields values
    size_t set_fields(const ::protoex_pmr::BalanceMessage& fbe_value) noexcept;

private:
    FBEBuffer& _buffer;
    mutable size_t _offset;

public:
    FinalModel<::protoex_pmr::Balance> body;
};

namespace protoex_pmr {

// Fast Binary Encoding BalanceMessage final model
class BalanceMessageFinalModel : public FBE::Model
{
public:
    BalanceMessageFinalModel() : _model(this->buffer(), 8) {}
    BalanceMessageFinalModel(const std::shared_ptr<FBEBuffer>& buffer) : FBE::Model(buffer), _model(this->buffer(), 8) {}

    // Get the model type
    static constexpr size_t fbe_type() noexcept { return FinalModel<::protoex_pmr::BalanceMessage>::fbe_type(); }

    // Check if the struct value is valid
    bool verify();

    // Serialize the struct value
    size_t serialize(const ::protoex_pmr::BalanceMessage& value);
    // Deserialize the struct value
    size_t deserialize(::protoex_pmr::BalanceMessage& value) const noexcept;
    // Deserialize the struct value with memory resource
    size_t deserialize(::protoex_pmr::BalanceMessage& value, std::pmr::memory_resource* resource) const noexcept;

    // Move to the next struct value
    void next(size_t prev) noexcept { _model.fbe_shift(prev); }

private:
    FinalModel<::protoex_pmr::BalanceMessage> _model;
};

} // namespace protoex_pmr

// Fast Binary Encoding ::protoex_pmr::AccountMessage final model
template <>
class FinalModel<::protoex_pmr::AccountMessage>
{
public:
    FinalModel(FBEBuffer& buffer, size_t offset) noexcept;

    // Get the allocation size
    size_t fbe_allocation_size(const ::protoex_pmr::AccountMessage& fbe_value) const noexcept;
    // Get the final offset
    size_t fbe_offset() const noexcept { return _offset; }
    // Set the final offset
    size_t fbe_offset(size_t offset) const noexcept { return _offset = offset; }
    // Get the final type
    static constexpr size_t fbe_type() noexcept { return 13; }

    // Shift the current final offset
    void fbe_shift(size_t size) noexcept { _offset += size; }
    // Unshift the current final offset
    void fbe_unshift(size_t size) noexcept { _offset -= size; }

    // Check if the struct value is valid
    size_t verify() const noexcept;
    // Check if the struct fields are valid
    size_t verify_fields() const noexcept;

    // Get the struct value
    size_t get(::protoex_pmr::AccountMessage& fbe_value) const noexcept;
    // Get the struct fields values
    size_t get_fields(::protoex_pmr::AccountMessage& fbe_value) const noexcept;
    // Get the struct value with memory resource
    size_t get(::protoex_pmr::AccountMessage& fbe_value, std::pmr::memory_resource* resource) const noexcept;
    // Get the struct fields values with memory resource
    size_t get_fields(::protoex_pmr::AccountMessage& fbe_value, std::pmr::memory_resource* resource) const noexcept;

    // Set the struct value
    size_t set(const ::protoex_pmr::AccountMessage& fbe_value) noexcept;
    // Set the struct fields values
    size_t set_fields(const ::protoex_pmr::AccountMessage& fbe_value) noexcept;

private:
    FBEBuffer& _buffer;
    mutable size_t _offset;

public:
    FinalModel<::protoex_pmr::Account> body;
};

namespace protoex_pmr {

// Fast Binary Encoding AccountMessage final model
class AccountMessageFinalModel : public FBE::Model
{
public:
    AccountMessageFinalModel() : _model(this->buffer(), 8) {}
    AccountMessageFinalModel(const std::shared_ptr<FBEBuffer>& buffer) : FBE::Model(buffer), _model(this->buffer(), 8) {}

    // Get the model type
    static constexpr size_t fbe_type() noexcept { return FinalModel<::protoex_pmr::AccountMessage>::fbe_type(); }

    // Check if the struct value is valid
    bool verify();

    // Serialize the struct value
    size_t serialize(const ::protoex_pmr::AccountMessage& value);
    // Deserialize the struct value
    size_t deserialize(::protoex_pmr::AccountMessage& value) const noexcept;
    // Deserialize the struct value with memory resource
    size_t deserialize(::protoex_pmr::AccountMessage& value, std::pmr::memory_resource* resource) const noexcept;

    // Move to the next struct value
    void next(size_t prev) noexcept { _model.fbe_shift(prev); }

private:
    FinalModel<::protoex_pmr::AccountMessage> _model;
};

} // namespace protoex_pmr

} // namespace FBE
