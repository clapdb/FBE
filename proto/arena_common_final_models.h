//------------------------------------------------------------------------------
// Automatically generated by the Fast Binary Encoding compiler, do not modify!
// https://github.com/chronoxor/FastBinaryEncoding
// Source: arena_common.fbe
// FBE version: 1.11.0.0
//------------------------------------------------------------------------------

#pragma once

#ifdef isset
#undef isset
#endif

#if defined(__clang__)
#pragma clang system_header
#elif defined(__GNUC__)
#pragma GCC system_header
#elif defined(_MSC_VER)
#pragma system_header
#endif

#include "fbe_final_models.h"

#include "arena_common.h"

namespace FBE {

// Fast Binary Encoding ::arena_common::Optr final model
template <>
class FinalModel<::arena_common::Optr> : public FinalModelBase<::arena_common::Optr, uint8_t>
{
public:
    using FinalModelBase<::arena_common::Optr, uint8_t>::FinalModelBase;
};

// Fast Binary Encoding ::arena_common::Expr final model
template <>
class FinalModel<::arena_common::Expr>
{
public:
    FinalModel(FBEBuffer& buffer, size_t offset) noexcept : _buffer(buffer), _offset(offset) {}

    // Get the allocation size
    size_t fbe_allocation_size(const ::arena_common::Expr& fbe_value) const noexcept;
    // Get the final offset
    size_t fbe_offset() const noexcept { return _offset; }
    // Set the final offset
    size_t fbe_offset(size_t offset) const noexcept { return _offset = offset; }

    // Shift the current final offset
    void fbe_shift(size_t size) noexcept { _offset += size; }
    // Unshift the current final offset
    void fbe_unshift(size_t size) noexcept { _offset -= size; }

    // Check if the variant value is valid
    size_t verify() const noexcept;

    // Get the variant value
    size_t get(::arena_common::Expr& fbe_value) const noexcept;

    // Set the variant value
    size_t set(const ::arena_common::Expr& fbe_value) noexcept;

private:
    FBEBuffer& _buffer;
    mutable size_t _offset;
};

// Fast Binary Encoding ::arena_common::Alias final model
template <>
class FinalModel<::arena_common::Alias>
{
public:
    FinalModel(FBEBuffer& buffer, size_t offset) noexcept;

    // Get the allocation size
    size_t fbe_allocation_size(const ::arena_common::Alias& fbe_value) const noexcept;
    // Get the final offset
    size_t fbe_offset() const noexcept { return _offset; }
    // Set the final offset
    size_t fbe_offset(size_t offset) const noexcept { return _offset = offset; }
    // Get the final type
    static constexpr size_t fbe_type() noexcept { return 1; }

    // Shift the current final offset
    void fbe_shift(size_t size) noexcept { _offset += size; }
    // Unshift the current final offset
    void fbe_unshift(size_t size) noexcept { _offset -= size; }

    // Check if the struct value is valid
    size_t verify() const noexcept;
    // Check if the struct fields are valid
    size_t verify_fields() const noexcept;

    // Get the struct value
    size_t get(::arena_common::Alias& fbe_value) const noexcept;
    // Get the struct fields values
    size_t get_fields(::arena_common::Alias& fbe_value) const noexcept;

    // Set the struct value
    size_t set(const ::arena_common::Alias& fbe_value) noexcept;
    // Set the struct fields values
    size_t set_fields(const ::arena_common::Alias& fbe_value) noexcept;

private:
    FBEBuffer& _buffer;
    mutable size_t _offset;

public:
    FinalModel<FBEString> name;
    FinalModel<::arena_common::Optr> optr;
    FinalModel<::arena_common::Expr> expr;
};

namespace arena_common {

// Fast Binary Encoding Alias final model
class AliasFinalModel : public FBE::Model
{
public:
    AliasFinalModel() : _model(this->buffer(), 8) {}
    AliasFinalModel(const std::shared_ptr<FBEBuffer>& buffer) : FBE::Model(buffer), _model(this->buffer(), 8) {}

    // Get the model type
    static constexpr size_t fbe_type() noexcept { return FinalModel<::arena_common::Alias>::fbe_type(); }

    // Check if the struct value is valid
    bool verify();

    // Serialize the struct value
    size_t serialize(const ::arena_common::Alias& value);
    // Deserialize the struct value
    size_t deserialize(::arena_common::Alias& value) const noexcept;

    // Move to the next struct value
    void next(size_t prev) noexcept { _model.fbe_shift(prev); }

private:
    FinalModel<::arena_common::Alias> _model;
};

} // namespace arena_common

// Fast Binary Encoding ::arena_common::Expression final model
template <>
class FinalModel<::arena_common::Expression>
{
public:
    FinalModel(FBEBuffer& buffer, size_t offset) noexcept;

    // Get the allocation size
    size_t fbe_allocation_size(const ::arena_common::Expression& fbe_value) const noexcept;
    // Get the final offset
    size_t fbe_offset() const noexcept { return _offset; }
    // Set the final offset
    size_t fbe_offset(size_t offset) const noexcept { return _offset = offset; }
    // Get the final type
    static constexpr size_t fbe_type() noexcept { return 2; }

    // Shift the current final offset
    void fbe_shift(size_t size) noexcept { _offset += size; }
    // Unshift the current final offset
    void fbe_unshift(size_t size) noexcept { _offset -= size; }

    // Check if the struct value is valid
    size_t verify() const noexcept;
    // Check if the struct fields are valid
    size_t verify_fields() const noexcept;

    // Get the struct value
    size_t get(::arena_common::Expression& fbe_value) const noexcept;
    // Get the struct fields values
    size_t get_fields(::arena_common::Expression& fbe_value) const noexcept;

    // Set the struct value
    size_t set(const ::arena_common::Expression& fbe_value) noexcept;
    // Set the struct fields values
    size_t set_fields(const ::arena_common::Expression& fbe_value) noexcept;

private:
    FBEBuffer& _buffer;
    mutable size_t _offset;

public:
    FinalModelVector<FBEString> keys;
    FinalModelVector<::arena_common::Alias> aliases;
    FinalModelMap<int32_t, ::arena_common::Alias> alias_int;
};

namespace arena_common {

// Fast Binary Encoding Expression final model
class ExpressionFinalModel : public FBE::Model
{
public:
    ExpressionFinalModel() : _model(this->buffer(), 8) {}
    ExpressionFinalModel(const std::shared_ptr<FBEBuffer>& buffer) : FBE::Model(buffer), _model(this->buffer(), 8) {}

    // Get the model type
    static constexpr size_t fbe_type() noexcept { return FinalModel<::arena_common::Expression>::fbe_type(); }

    // Check if the struct value is valid
    bool verify();

    // Serialize the struct value
    size_t serialize(const ::arena_common::Expression& value);
    // Deserialize the struct value
    size_t deserialize(::arena_common::Expression& value) const noexcept;

    // Move to the next struct value
    void next(size_t prev) noexcept { _model.fbe_shift(prev); }

private:
    FinalModel<::arena_common::Expression> _model;
};

} // namespace arena_common

} // namespace FBE
